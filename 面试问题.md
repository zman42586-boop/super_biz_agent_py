# SuperBizAgent 项目面试问题与答案

---

## 一、RAG + Milvus 知识库

### Q1: 介绍一下你在这个项目中 RAG 的整体架构，文档是怎么入库和检索的？

**答：**

整体流程分两阶段：

**入库阶段：**
1. 通过 `POST /api/upload` 上传 `*.md` / `*.txt` 文件。
2. `DocumentSplitterService` 做文档切分 —— 按 `CHUNK_MAX_SIZE`（默认800字符）切分，设 `CHUNK_OVERLAP`（默认100字符）保证语义连贯。切分时同时提取 Markdown 标题层级（h1/h2/h3）写入 metadata，检索时可用于定位来源。
3. Embedding 模型（`sentence-transformers`，如 `BAAI/bge-small-zh-v1.5`）将 chunk 向量化。
4. 向量 + 原文 + metadata 存入 Milvus。

**检索阶段：**
- `retrieve_knowledge` 是一个 LangChain `@tool`，`response_format="content_and_artifact"`，返回格式化文本和原始 Document 列表两端。
- 用 `vector_store.as_retriever(search_kwargs={"k": config.rag_top_k})`（默认 top 3）做相似度检索。
- 格式化时把来源文件名、标题层级拼接进上下文，给 LLM 提供可引用出处。
- **Agentic RAG**：不是对话一开始就检索，而是 LLM 在需要时主动调用该 tool，减少无关检索噪声。

### Q2: 为什么选 Milvus 而不是其他向量数据库（如 Chroma、Pinecone、Weaviate）？

**答：**

| 维度 | Milvus | Chroma | Pinecone |
|------|--------|--------|----------|
| 部署模式 | 分布式，支持 standalone/cluster | 嵌入式/轻量 C/S | SaaS 托管 |
| 索引算法 | IVF_FLAT、HNSW、DiskANN 等 | 基础 ANN | 专有索引 |
| 规模 | 十亿级向量 | 百万级 | 按套餐 |
| 生态 | CNCF 孵化、LangChain 原生支持 | 轻量友好 | 商业产品 |

选择 Milvus 的原因：
- 企业级场景下向量量级可能很大，Milvus 的分布式能力和多种索引选择（HNSW 适合低延迟、IVF_FLAT 适合大规模）可以横向扩展。
- `langchain-milvus` 和 `pymilvus` 的集成成熟，`as_retriever()` 一行接入。
- Docker Compose 一键启动 standalone（`vector-database.yml`），开发运维都方便。

### Q3: 文档切分的 chunk_size 和 overlap 怎么定的？有没有理论依据？

**答：**

当前默认配置 `CHUNK_MAX_SIZE=800`、`CHUNK_OVERLAP=100`。但实际的切分策略比这一组数字复杂——代码里是一个**三阶段管线**：

**阶段一：语义切分（Markdown 标题分割）**

`MarkdownHeaderTextSplitter` 先按 `#`（h1）和 `##`（h2）把文档切成语义块。只按一级和二级标题分，**故意不分三级标题**——注释写明"减少分片数，避免过度碎片化"。同时 `strip_headers=False` 保留标题在 chunk 内容里，让每个 chunk 自带"它是哪一节的"上下文。

**阶段二：长度切分（递归字符分割）**

语义块如果还太长，`RecursiveCharacterTextSplitter` 上场。注意它用的 `chunk_size` 是 `self.chunk_size * 2 = 1600`，不是 800。因为阶段一已经按标题语义切过了，阶段二只需处理仍然超长的块，size 加倍可以进一步减少分片数。

`RecursiveCharacterTextSplitter` 的分隔符优先级是 `["\n\n", "\n", " ", ""]`——优先在段落边界切，其次在换行处，最后才是字符级硬切。尽量保证 chunk 是完整的段落，而不是在半句话中间断开。

**阶段三：合并小片段**

切完之后，小于 300 字符的 chunk 会跟相邻 chunk 合并（只要合并后不超过 `chunk_size * 2 = 1600`）。防止出现"一句话一个 chunk"的碎片化问题。

**所以 800 这个数字的依据是什么？**

1. **Embedding 模型的训练分布**：`BAAI/bge-small-zh-v1.5` 训练时 passage 长度典型在 256-512 tokens（约 400-800 中文字符）。chunk 大小跟模型训练时的 passage 分布对齐，向量表征质量最高。如果 chunk 是 3000 字符，Embedding 模型需要把一大段话压缩成 512 维向量，信息密度过高会有损失。

2. **检索精度 vs 召回率的平衡**：chunk 越小，检索精度越高（匹配到的 chunk 跟 query 相关性强），但越容易丢失上下文导致召回不全。800 字符大约是中文的 2-3 个段落，刚好是一个完整论述单元的大小——一个概念、一条运维知识基本能说清楚。

3. **答案拼接的可用性**：默认 `RAG_TOP_K=3`，3 个 800 字符的 chunk 加上 prompt 其他部分，总共约 3000-4000 字符，模型处理起来轻松，不会因为塞太多上下文而注意力衰减。如果 chunk 2000 字符，3 个就 6000+，开始有"lost in the middle"风险。

4. **overlap=100 的考量**：100/800 ≈ 12.5% 重叠率，保证相邻 chunk 在边界处至少有一句半句的语义重叠。`RecursiveCharacterTextSplitter` 在段落边界切分时 overlap 天然保证了"被切开的段落会在上一个 chunk 结尾和下一个 chunk 开头各出现一次"，检索时无论命中哪一侧都能拿到完整语义。

**追问一：怎么评估 chunk_size 设得合不合理？有哪些量化指标？**

回答这个问题前先说清一个前提：**检索质量 ≠ 端到端问答质量**。检索阶段（Retrieval）和生成阶段（Generation）应该分开评估，否则你不知道问题出在"没搜到"还是"搜到了但 LLM 没用对"。

**核心量化指标：**

| 指标 | 公式 | 衡量什么 | 什么时候更关注 |
|------|------|----------|---------------|
| **Recall@k** | 正确 chunk 在 top-k 中 / 全部正确 chunk | 检索"找全"的能力 | 不能漏掉关键信息（诊断场景） |
| **Precision@k** | top-k 中正确的 / k | 检索"找对"的能力 | 不能给 LLM 塞噪声（闲聊场景） |
| **MRR** (Mean Reciprocal Rank) | 1 / 第一个正确 chunk 的排名 | 正确答案排得有多靠前 | 关心 LLM 第一眼看到啥 |
| **NDCG@k** (Normalized DCG) | 考虑排名位置的加权命中率 | 排序整体质量 | 既看重命中、又看重顺序 |

以本项目为例，RAG 默认 `RAG_TOP_K=3`，关键指标是 **Recall@3** 和 **MRR**：

- **Recall@3**：一条运维知识的标准答案，能否出现在 Milvus 返回的 3 个 chunk 里？运维诊断怕的是"漏掉关键证据"，所以 Recall 比 Precision 更重要——宁可多搜几条让 LLM 自己筛选，也不能缺失关键信息。
- **MRR**：第一个正确 chunk 排在第几位？如果总排在 k=2 或 k=3，说明 Embedding 模型的语义匹配不够准，可能需要换模型或做重排（Reranker）。如果排在第 1 位，表示向量相似度直接命中了最佳答案。

**Precision@k 和 Recall@k 的取舍：**

- 客服问答场景：Precision@k 更重要。用户问"怎么退货"，检索返回一堆物流政策、换货规则、会员权益——全是噪声，反而干扰 LLM 回答。
- 运维诊断场景：Recall@k 更重要。CPU 飙高的根因可能藏在监控指标、错误日志、配置变更记录的任何一个 chunk 里，漏掉一条就找不到真相。

**追问二：这些指标怎么测？需要自己构建测试集吗？**

必须自己构建。不存在通用的"chunk_size 基准测试集"，因为每个项目的文档结构、业务语义、用户提问方式都不同。构建方式：

**1. 准备测试 query 集合（50-100 条起步）：**

从真实场景中采集问题——运维场景可以从历史工单、告警描述、运维手册 FAQ 中提取。例如：
```
"CPU 使用率超过 80% 如何排查？"
"内存泄漏的常见原因有哪些？"
"服务启动失败怎么查日志？"
```

**2. 人工标注正确答案（Ground Truth）：**

对每个 query，去你的知识库里圈定"哪些 chunk 包含这个问题的答案"。这是最耗时但最关键的步骤。标注时注意：
- 一个 query 可能对应多个正确 chunk（Recall 计算时需要考虑全部）。
- 标注粒度：是 chunk 级别还是段落级别？chunk 级别标注最直接，因为 Milvus 检索返回的就是 chunk。
- 至少做 2-3 条标注以验证一致性（标注歧义会导致指标失真）。

**3. 跑检索、算指标：**

```python
# 伪代码示例
for query, ground_truth_chunks in test_set:
    retrieved = vector_store.similarity_search(query, k=k)  # k=3/5/10
    chunk_ids = [doc.metadata["chunk_id"] for doc in retrieved]
    
    recall = len(set(chunk_ids) & set(ground_truth_chunks)) / len(ground_truth_chunks)
    precision = len(set(chunk_ids) & set(ground_truth_chunks)) / k
    
    # MRR: 第一个正确chunk出现在第几位
    for rank, cid in enumerate(chunk_ids, 1):
        if cid in ground_truth_chunks:
            mrr = 1 / rank
            break
```

**4. 调参对比：**

用同一份测试集跑不同参数组合，画对比表：

| chunk_size | overlap | Recall@3 | MRR |
|-----------|---------|----------|-----|
| 400 | 50 | 0.72 | 0.58 |
| 800 | 100 | **0.85** | **0.71** |
| 1600 | 200 | 0.78 | 0.63 |

一眼就能看出 800+100 是当前最优。面试官听到这里就知道你不是"拍脑袋定的参数"。

**5. 加 Reranker（重排器）后的评估：**

如果有 Reranker（如 `BAAI/bge-reranker-v2-m3`），评估也分两轮：

- **Stage 1 召回**：向量检索粗排，用 Recall@k（k 大一点，如 k=10）保证召回率。
- **Stage 2 重排**：Reranker 精排后，用 **MRR** 和 **NDCG@k**（k 小一点，如 k=3）评估排序质量是否改善。

面试官说"我们也用 Reranker"，你就可以接："那你们的 Reranker 上线前后 MRR 变化了多少？"——这一步讨论就从"用了什么"升级到"效果怎么样"。

**补充：如果暂时没有标注资源怎么办？**

- **LLM-as-Judge**：用 GPT-4 等强模型做自动打分（给 query + retrieved chunks，让 LLM 判断相关度 1-5 分）。成本低但有一定噪声，适合快速迭代。
- **A/B 线上观测**：不改代码，只记录不同 chunk_size 配置下的用户点赞/点踩率、追问率（用户不满意说明检索没到位）。虽然粗糙但反映了真实用户体验。

### Q4: 什么是 Agentic RAG？和传统 RAG 有什么区别？

**答：**

- **传统 RAG**：每次对话固定流程 —— 用户问题 → 检索 → 拼接上下文 → LLM 回答。不管问题是否需要检索，都会查一次向量库。缺点是：闲聊也检索、无关文档也塞给 LLM 造成干扰。
- **Agentic RAG**：检索被封装成一个 Tool，由 Agent 自己决定什么时候调用、用什么 query 调用、调用几次。本项目就是这种模式 —— Planner 在制定计划时如果判断需要知识支撑，就调用 `retrieve_knowledge`；如果不需要（比如简单问候），就不调。

**优势**：
- 减少无关检索噪声
- LLM 可以根据中间结果改写 query 再次检索（迭代检索）
- 更灵活，检索策略可随任务动态调整

### Q5 (八股): Embedding 模型选型，BGE 系列有什么特点？为什么用 bge-small 而不是 bge-large？

**答：**

BGE（BAAI General Embedding）是智源研究院开源的中文 Embedding 模型系列，特点：
- **针对检索任务优化**：训练时使用对比学习，query 和 passage 分别编码，检索效果好。
- **中文能力强**：BAAI 专门针对中文场景训练，中文语义理解优于多语言通用模型。
- **支持指令微调**：BGE 模型的 query 侧可以加 instruction prefix（如 "为这个句子生成表示以用于检索相关文章："），提升检索质量。

选择 `bge-small-zh-v1.5`（约 24M 参数）而非 `bge-large`（约 326M）的原因：
- small 推理速度快 10 倍以上，CPU 上也能跑。
- 向量维度 512（vs large 的 1024），Milvus 存储和检索开销更低。
- 中文短文本检索场景精度差距不大（MTEB 中文榜单 small 和 large 的差距在 3% 以内），性价比高。
- 更适合开发环境和资源受限的边缘部署。

---

## 二、Plan-Execute-Replanner 诊断流程

### Q6: 详细描述一下 Plan-Execute-Replanner 的工作流程，每一步做什么？

**答：**

基于 LangGraph 的 `StateGraph`，4 个节点 + 1 个条件边：

```
Planner → Executor → Microcompact → Replanner → 条件判断 → END / Executor
```

**1. Planner（规划器）**
- 注入 Hot Memory（`memory/MEMORY.md`）、Skill 描述（`.claude/skills/*.md`）、可用工具列表（本地工具 + MCP 工具）。
- **按需调用 `retrieve_knowledge`** 获取相关知识上下文。
- LLM 用 `with_structured_output(Plan, method="function_calling")` 输出结构化计划（`Plan.steps: List[str]`）。
- 异常时回退到默认 3 步计划（检查系统状态 → 分析日志 → 给出诊断结论）。

**2. Executor（执行器）**
- 取出 `plan[0]` 作为当前任务。
- 组装工具列表（本地 + MCP，MCP 不可用时优雅降级）。
- `llm.bind_tools(all_tools)` → 判断是否需要 tool call。
- 有 tool call → `ToolNode` 自动执行工具 → 把结果再喂给 LLM 做最终合成。
- 无 tool call → 直接用 LLM 回复。
- 追加 `(task, result)` 到 `past_steps`，`plan` 变为 `plan[1:]`。
- 报错不抛异常，而是把错误信息作为 step result 写回 `past_steps`。

**3. Microcompact（微压缩）**
- 检查最新 `past_steps[-1]` 的 token 数是否超过阈值（100 tokens）。
- 超过 → `aiofiles` 异步写入 `memory/artifacts/{session_id}/{step}_{timestamp}.txt`。
- 把 `past_steps` 最后一步替换为短摘要 + 文件路径引用。
- 目标：防止大工具结果（如几百行日志）撑爆 Replanner 上下文。

**4. Replanner（重规划器）**
- 三种决策优先级：**respond** > **continue** > **replan**。
- 硬约束：`MAX_STEPS=8`（总步数上限）、5 步后禁止 replan。
- 如果要 respond → `_generate_response()` 生成最终诊断报告。
- Collapse 压缩（Level 3）：当 `past_steps` 过多时，旧步骤折叠为单行摘要，仅保留最近 2 步详情。
- 条件边 `should_continue()`：有 response → END；plan 非空 → Executor；否则 → END。

**循环结束后**：

- `memory_writer.save_incident()` 把完整报告写入 `memory/incidents/`，更新 `memory/MEMORY.md` 索引。

### Q7: LangGraph 的 StateGraph 和条件边是怎么设计的？为什么要用自定义 reducer？

**答：**

**StateGraph 定义：**
```python
graph = StateGraph(PlanExecuteState)
graph.add_node("planner", planner)
graph.add_node("executor", executor)
graph.add_node("microcompact", microcompact)
graph.add_node("replanner", replanner)
graph.add_edge(START, "planner")
graph.add_edge("planner", "executor")
graph.add_edge("executor", "microcompact")
graph.add_edge("microcompact", "replanner")
graph.add_conditional_edges("replanner", should_continue, {
    "executor": "executor",
    END: END,
})
```

**条件边的逻辑**（`should_continue`）：
- `state["response"]` 非空 → `END`（诊断完成）
- `state["plan"]` 非空 → `executor`（继续执行剩余步骤）
- 否则 → `END`（异常终止）

**为什么用自定义 `_replace` reducer？**

LangGraph 默认对 list 类型的 state 字段使用 append 语义。但 `past_steps` 有两个特殊需求：
1. **Microcompact 需要替换最后一步**（不是追加）。当工具结果过大时，需要把原文替换为摘要 + 文件路径。
2. **Executor 需要全量替换**。Microcompact 替换后，Executor 基于当前全量列表追加新 step。

所以自定义了 `_replace` reducer：
```python
def _replace(current, update):
    return update  # 全量替换，不是追加
```

没有这个 reducer，Microcompact 的替换会变成追加，导致 `past_steps` 同时包含原文和摘要。

### Q8: 防死循环和确保终止有哪些手段？

**答：**

1. **`MAX_STEPS = 8`**：硬上限，`past_steps` 长度 >= 8 时，Replanner **强制 respond**，不能再 replan/continue。
2. **5 步后禁止 replan**：`len(past_steps) >= 5` 时，不允许 action="replan"，防止无限重规划。
3. **replan 步数截断**：即使 Replanner 返回了新的 `new_steps`，也会被 `剩余步数 = MAX_STEPS - len(past_steps)` 截断，确保不超限。
4. **空 plan 终止**：`should_continue()` 中，如果 plan 为空且无 response，走 END。
5. **Token 级别的压缩**：Level 1（Snip 截断旧消息）、Level 2（Microcompact 大结果落盘）、Level 3（Collapse 折叠旧步骤）、Level 4（Autocompact 监控）—— 四层压缩确保上下文永远可控。

### Q9 (八股): LangGraph 和 LangChain 的关系是什么？为什么要用 LangGraph 而不是纯 LangChain Chain？

**答：**

- **LangChain** 是高层框架，提供了 Chain、Agent、Tool 等抽象。早期的 Chain（如 LLMChain、SequentialChain）适合 DAG（有向无环图）场景。
- **LangGraph** 是 LangChain 生态中的图编排引擎，支持**循环、条件分支、状态持久化**。

选择 LangGraph 的原因：
1. **Plan-Execute-Replanner 本质是一个有环图**：Executor → Replanner → Executor 是一个循环，LangChain 的 Chain 不支持环。
2. **条件路由**：Replanner 需要根据当前状态决定 continue/replan/respond，LangGraph 的 `conditional_edges` 天然支持。
3. **State 管理**：`PlanExecuteState` 在图中流转，每个节点只读写自己关心的字段，`MemorySaver` checkpointer 自动持久化。
4. **流式输出**：`graph.astream()` 原生支持流式事件，SSE 推送给前端，用户可看到诊断进度。

**补充：LangGraph vs 传统 Agent 循环**
- 传统 Agent 循环（Thought → Action → Observation → ...）是隐式循环，逻辑封装在 AgentExecutor 内部。
- LangGraph StateGraph 是显式图，每个节点的输入/输出/转移条件都是清晰的，更容易调试、扩展和加中间压缩节点。

---

## 三、MCP 监控工具解耦（深入源码级分析）

### Q10: 项目中 MCP 工具的底层是什么？用的什么库？什么协议？

**答：**

MCP 工具链由三部分组成，底层有两个核心库：

| 层级 | 库 | 版本 | 作用 |
|------|-----|------|------|
| **Server 端** | `fastmcp` | >=2.14.0 | 构建 MCP Server，提供 `@mcp.tool()` 装饰器自动将 Python 函数暴露为 MCP 工具。内部通过 `FastMCP` 类管理 Server 生命周期 |
| **Client 端** | `langchain-mcp-adapters` | >=0.2.1 | LangChain 生态的 MCP 适配层，核心类是 `MultiServerMCPClient`。将远程 MCP 工具包装为 LangChain 的 `BaseTool` 对象，LLM 可以像调用本地 `@tool` 一样调用它们 |
| **传输层** | MCP 协议 `streamable-http` | — | JSON-RPC over HTTP，支持流式请求/响应。Server 暴露 HTTP 端点，Client 通过 HTTP 连接并调用 `list_tools()`、`call_tool()` 等 MCP 标准方法 |

**底层通信协议栈：**

```
Python 函数  (@mcp.tool() 装饰器注册)
    ↓
FastMCP 框架 (自动提取 name/description/parameters JSON Schema)
    ↓
MCP 协议 (JSON-RPC 2.0 over HTTP)
    ↓
streamable-http transport (HTTP POST/GET 流式传输)
    ↓
MultiServerMCPClient (langchain-mcp-adapters)
    ↓
LangChain BaseTool (LLM 可调用的标准工具)
    ↓
LLM function calling (OpenAI tool_choice 协议)
```

**为什么选 `fastmcp`？**

`fastmcp` 是 MCP Python SDK 的高层封装，比底层 `mcp` 包更简洁。关键优势：
- `@mcp.tool()` 一个装饰器就完成工具注册，自动从函数签名 + docstring 提取参数 Schema
- `mcp.run(transport="streamable-http", ...)` 一行代码启动 HTTP Server
- 内置日志、错误处理、参数校验

**为什么选 `langchain-mcp-adapters`？**

因为本项目 LangChain/LangGraph 是主体框架。`MultiServerMCPClient` 把 MCP 工具的发现、调用全部适配为 LangChain 原生接口：
- `client.get_tools()` → 返回 `List[BaseTool]`
- 返回的 Tool 可以直接传给 `llm.bind_tools()`
- 可以直接放进 LangGraph 的 `ToolNode`

---

### Q11: MCP 工具是怎么注册的？从声明到 LLM 可调用的完整链路是什么？

**答：**

注册分三个环节：Server 端注册（代码）→ 配置声明（yaml/env）→ Client 端拉取（运行时）。

#### 环节一：Server 端 — `@mcp.tool()` 装饰器注册

代码位置：`mcp_servers/monitor_server.py`

```python
from fastmcp import FastMCP

mcp = FastMCP("Monitor")  # ① 创建名为 "Monitor" 的 MCP Server

@mcp.tool()               # ② 装饰器：自动注册为 MCP 工具
@log_tool_call            # ③ 自定义装饰器：记录调用日志
def query_cpu_metrics(
    service_name: str,                # ← 自动变为 required 参数
    start_time: Optional[str] = None, # ← 自动变为 optional 参数
    end_time: Optional[str] = None,
    interval: str = "1m",
) -> Dict[str, Any]:
    """查询服务的 CPU 使用率监控数据。       ← docstring → tool description
    ...
    """

# ④ 启动 Server：监听 8004 端口
if __name__ == "__main__":
    mcp.run(transport="streamable-http", host="127.0.0.1", port=8004, path="/mcp")
```

`FastMCP` 框架在背后做了什么：
1. 扫描被 `@mcp.tool()` 装饰的函数
2. 从函数签名提取参数名、类型、默认值 → 生成 JSON Schema
3. 从函数 docstring 提取描述文本
4. 暴露 MCP 标准端点（`list_tools`、`call_tool` 等）

启动后，外部 Client 访问 `http://localhost:8004/mcp` 即可发现和调用这些工具。

#### 环节二：配置层 — 声明 MCP Server 地址

代码位置：`app/config.py:46-82`

```python
class Settings(BaseSettings):
    # 从 .env 文件读取配置
    mcp_monitor_transport: str = "streamable-http"
    mcp_monitor_url: str = "http://localhost:8004/mcp"

    @property
    def mcp_servers(self) -> Dict[str, Dict[str, Any]]:
        """构建 MultiServerMCPClient 所需的 servers 配置字典"""
        return {
            "monitor": {                             # Server 别名（逻辑名）
                "transport": self.mcp_monitor_transport,  # 传输协议
                "url": self.mcp_monitor_url,              # Server 地址
            }
        }
```

对应的 `.env` 环境变量：
```
MCP_MONITOR_TRANSPORT=streamable-http
MCP_MONITOR_URL=http://localhost:8004/mcp
```

> `.env` 中还预留了 `MCP_CLS_TRANSPORT` / `MCP_CLS_URL`（腾讯云 CLS 日志服务），但当前 `config.py` 的 `mcp_servers` 属性**未注册该 Server**，处于预留未启用状态。如需接入，只需在 `mcp_servers` 属性中新增条目。

#### 环节三：Client 端 — 运行时拉取工具列表

代码位置：`app/agent/mcp_client.py:84-127`

```python
# ① 模块加载时读取配置
DEFAULT_MCP_SERVERS = config.mcp_servers

# ② 全局单例
_mcp_client: Optional[MultiServerMCPClient] = None

async def get_mcp_client(servers=None, tool_interceptors=None, force_new=False):
    global _mcp_client
    if _mcp_client is None or force_new:
        _mcp_client = _create_mcp_client(servers or DEFAULT_MCP_SERVERS, ...)
    return _mcp_client

def _create_mcp_client(servers, tool_interceptors):
    # ③ servers 配置直接传入 MultiServerMCPClient 构造函数
    return MultiServerMCPClient(servers, **kwargs)
```

`MultiServerMCPClient(servers)` 的构造过程：
1. 遍历 `servers` 字典，为每个 Server 建立 HTTP 连接
2. 向每个 Server 发起 `list_tools` 请求（MCP 协议的 JSON-RPC 调用）
3. 将返回的工具列表包装为 `langchain_core.tools.BaseTool` 对象

#### 运行时拉取 — 各节点调用 `get_tools()`

代码位置：`app/agent/aiops/executor.py:52-58`

```python
# Planner / Executor / Replanner 三个节点都会调用
mcp_client = await get_mcp_client_with_retry()
mcp_tools = await mcp_client.get_tools()  # ← 返回 List[BaseTool]

# 合并本地工具 + MCP 工具
all_tools = local_tools + mcp_tools
```

**完整注册链路总结：**

```
① 写 Python 函数 + @mcp.tool() 装饰器 (monitor_server.py)
        ↓
② 启动独立进程 (python monitor_server.py) → 监听 8004
        ↓
③ .env 配置 URL + config.py 构建 mcp_servers 字典
        ↓
④ MultiServerMCPClient(servers) 连接 → list_tools() → 包装为 LangChain Tool
        ↓
⑤ executor.py: all_tools = local_tools + mcp_tools
        ↓
⑥ llm.bind_tools(all_tools) → LLM 可调用
```

---

### Q12: MCP 工具是怎么被调用的？完整的调用链路是什么？

**答：**

以 AIOps 诊断为例，一次完整的 MCP 工具调用经历了 **7 个步骤**。

#### 入口：用户触发诊断

```
POST /api/aiops → api/aiops.py → AIOpsService.execute() → LangGraph 工作流
```

#### LangGraph 工作流

```
Planner → Executor → Microcompact → Replanner → (条件边) → Executor / END
```

#### Executor 节点：工具调用的核心

代码位置：`app/agent/aiops/executor.py:18-124`

```python
async def executor(state: PlanExecuteState) -> Dict[str, Any]:
    # 步骤①: 获取当前待执行的任务（plan 的第一个步骤）
    task = plan[0]  # 如 "使用 query_cpu_metrics 工具查询 data-sync-service 的 CPU 使用率"

    # 步骤②: 获取本地工具（3个）
    local_tools = [get_current_time, retrieve_knowledge, search_log]

    # 步骤③: 获取 MCP 客户端（单例 + 重试拦截器），拉取远程工具
    mcp_client = await get_mcp_client_with_retry()
    mcp_tools = await mcp_client.get_tools()  # 4 个 MCP 工具

    # 步骤④: 合并所有工具 → 7 个
    all_tools = local_tools + mcp_tools

    # 步骤⑤: 绑定工具到 LLM（转为 OpenAI function calling schema）
    llm = llm_factory.create_chat_model(temperature=0)
    llm_with_tools = llm.bind_tools(all_tools)
    #   ↑ bind_tools 做了：
    #   1. 遍历每个 BaseTool，提取 name/description/args_schema
    #   2. 组装为 OpenAI 兼容的 tools 参数:
    #      [{"type": "function", "function": {"name": "...", "description": "...", "parameters": {...}}}, ...]
    #   3. 随每次 LLM 请求一起发送

    # 步骤⑥: LLM 决策 — 是否需要调工具？调哪个？传什么参数？
    messages = [SystemMessage(...), HumanMessage(content=f"请执行: {task}")]
    llm_response = await llm_with_tools.ainvoke(messages)

    if llm_response.tool_calls:  # LLM 决定调用工具
        # 步骤⑦: ToolNode 自动执行工具
        tool_node = ToolNode(all_tools)
        tool_messages = await tool_node.ainvoke({"messages": messages + [llm_response]})
        # ToolNode 内部流程：
        #   对每个 tool_call → 匹配工具名 → 执行 tool.invoke(tool_call["args"])
        #   对于 MCP 工具 → 触发 retry_interceptor → HTTP 请求 MCP Server → 返回结果
        #   对于本地工具 → 直接执行 Python 函数

        # 步骤⑧: 工具结果回传 LLM 生成最终答案
        messages.extend(tool_messages["messages"])
        final_response = await llm_with_tools.ainvoke(messages)
        result = final_response.content
    else:
        # 无需工具，直接用 LLM 回复
        result = llm_response.content

    # 步骤⑨: 写入 past_steps，移除 plan[0]
    return {"plan": plan[1:], "past_steps": past_steps + [(task, result)]}
```

#### 一次 MCP 工具调用的 HTTP 层面发生了什么

当 LLM 决定调用 `query_cpu_metrics(service_name="data-sync-service")` 时：

```
1. LangChain ToolNode 调用 tool.invoke({"service_name": "data-sync-service"})
        ↓
2. MultiServerMCPClient 拦截调用 → retry_interceptor 包装
        ↓
3. retry_interceptor 调用 handler(request)
        ↓
4. MultiServerMCPClient 向 http://localhost:8004/mcp 发送 JSON-RPC 请求：
   {
     "jsonrpc": "2.0",
     "method": "tools/call",
     "params": {
       "name": "query_cpu_metrics",
       "arguments": {"service_name": "data-sync-service"}
     }
   }
        ↓
5. Monitor MCP Server (FastMCP) 接收请求 → 路由到 query_cpu_metrics 函数
        ↓
6. Python 函数执行（psutil 读取真实 CPU 数据）→ 返回 Dict
        ↓
7. FastMCP 将返回值序列化为 JSON → HTTP Response
        ↓
8. MultiServerMCPClient 解析响应 → 包装为 ToolMessage
        ↓
9. ToolMessage 追加到对话 → LLM 看到工具结果 → 生成最终回复
```

#### 重试拦截器（`retry_interceptor`）

代码位置：`app/agent/mcp_client.py:18-74`

```python
async def retry_interceptor(request, handler, max_retries=3, delay=1.0):
    """指数退避重试，全失败后返回错误结果而不是抛异常"""
    for attempt in range(max_retries):
        try:
            return await handler(request)
        except Exception as e:
            if attempt < max_retries - 1:
                wait_time = delay * (2 ** attempt)  # 1s → 2s → 4s
                await asyncio.sleep(wait_time)

    # 全部重试失败 → 返回 CallToolResult(isError=True)
    return CallToolResult(
        content=[TextContent(type="text", text=error_msg)],
        isError=True
    )
```

关键设计点：
- **不抛异常**：全失败后返回 `isError=True` 的结果，让 LLM 知道"这个工具调用失败了"而不是直接崩溃
- **指数退避**：1s → 2s → 4s，避免在 MCP Server 短暂不可用时频繁重试

#### Planner 和 Replanner 中的 MCP 工具

Planner 和 Replanner 也获取 MCP 工具列表，但**只用于生成描述文本**，让 LLM 知道"有哪些工具可用"，不做实际调用：

```python
# planner.py 和 replanner.py 中
mcp_tools = await mcp_client.get_tools()
all_tools = local_tools + mcp_tools
tools_description = format_tools_description(all_tools)
# → 生成文本: "- query_cpu_metrics: 查询服务的 CPU 使用率监控数据\n- ..."
# → 注入 System Prompt 供 LLM 制定/调整计划时参考
```

#### 优雅降级

如果 MCP Server 没启动或连不上：

```python
try:
    mcp_client = await get_mcp_client_with_retry()
    mcp_tools = await mcp_client.get_tools()
except Exception as e:
    logger.warning(f"获取 MCP 工具失败: {e}")
    mcp_tools = []  # 降级为空列表
```

MCP 工具不可用时，Agent 仍然可以用 3 个本地工具完成诊断，不会因为 MCP 故障而完全中断。

---

### Q13: 项目中有哪些 MCP 工具？各自做什么？

**答：**

#### MCP 工具（来自 Monitor Server，共 4 个）

全部定义在 `mcp_servers/monitor_server.py`：

| 工具名 | 功能 | 关键实现 | 行号 |
|--------|------|----------|------|
| `query_cpu_metrics` | 查询服务的 CPU 使用率监控数据 | `psutil.cpu_percent()` 读取真实 CPU + 生成时间序列数据点 + 统计信息（avg/max/min/p95）+ 告警判断 | 126 |
| `query_memory_metrics` | 查询服务的内存使用监控数据 | `psutil.virtual_memory()` 读取真实内存 + 时间序列 + 统计 + 告警判断（>70% 触发） | 263 |
| `list_lhm_sensors` | 列出 LibreHardwareMonitor 所有温度传感器 | HTTP GET `{lhm_base_url}/data.json` → 递归遍历 Children 找 Type="Temperature" 的节点 | 444 |
| `get_lhm_temperature` | 获取指定传感器的当前温度值 | 基于 `list_lhm_sensors` 的结果按名称匹配 + 温度阈值告警（默认 50°C，环境变量 `ONCALL_TEMP_THRESHOLD_C` 可配置） | 461 |

**参数契约**：

```python
# 示例：query_cpu_metrics 的参数签名
query_cpu_metrics(
    service_name: str,          # 必填
    start_time: Optional[str],  # 可选，格式 "YYYY-MM-DD HH:MM:SS"
    end_time: Optional[str],    # 可选
    interval: str = "1m"        # 可选，可选值 "1m"/"5m"/"1h"
)
# → FastMCP 自动将以上转为 JSON Schema 暴露给 Client

# 示例：get_lhm_temperature 的参数签名
get_lhm_temperature(
    sensor_name_contains: str = "CCDs Max (Tdie)",  # 可选，传感器名称关键字
    lhm_base_url: str = "http://127.0.0.1:8085"      # 可选
)
```

#### 本地工具（直接在 LangChain 内实现的 @tool，共 3 个）

| 工具名 | 功能 | 定义位置 |
|--------|------|----------|
| `get_current_time` | 获取当前时间（用于 LLM 感知时间） | `app/tools/time_tool.py` |
| `retrieve_knowledge` | 从 Milvus 向量知识库检索运维文档（Agentic RAG） | `app/tools/knowledge_tool.py` |
| `search_log` | 从 `logs/app_*.log` 按时间范围和关键词检索运行日志 | `app/tools/log_tool.py` |

#### 运行时工具的合并

Executor 中，最终 LLM 看到 7 个工具（3 本地 + 4 MCP），全部通过 `llm.bind_tools()` 转为 OpenAI function calling schema 发送给 LLM。

---

### Q14 (八股): MCP 和传统的 Function Calling / Plugin 有什么区别？

**答：**

| 维度 | MCP | Function Calling | Plugin（如 ChatGPT Plugin） |
|------|-----|------------------|---------------------------|
| 标准化 | 开放协议，任何 LLM 可实现 | 各厂商自有格式 | OpenAI 专有规范 |
| 工具发现 | 动态 `list_tools()` | 需在请求中显式声明 | manifest 注册 |
| 传输层 | stdio / streamable-http | API 请求体嵌入 | HTTP + manifest |
| 资源/提示 | 支持 Resources 和 Prompts | 仅 Tools | 仅 Tools |
| 解耦程度 | Server 独立进程部署 | 与 LLM 调用耦合 | 与 OpenAI 平台耦合 |
| 工具定义 | Python 函数 + 装饰器，自动提取 Schema | 手动写 JSON Schema | manifest 文件 |

**MCP 的核心优势：**

1. **Client-Server 分离**：工具服务可以独立开发、部署、扩展，不依赖 LLM 进程。本项目 Monitor MCP 挂了不影响 FastAPI 主进程
2. **动态发现**：Client 启动时自动调用 `list_tools()` 获取可用工具列表，新增工具无需改 Agent 代码，重启 MCP Server 即可
3. **多 Server 支持**：`MultiServerMCPClient` 可以同时连接多个 MCP Server，本项目 `.env` 已预留 CLS 日志 MCP 配置
4. **生态互通**：MCP 是开放协议，不同厂商的工具可以互操作，不绑定特定平台

---

## 四、主动轮询与 Webhook 告警

### Q15: 描述一下告警链路的完整流程，从指标采集到邮件通知。

**答：**

```
┌──────────────────────┐
│  LibreHardwareMonitor │  ← 硬件监控（Windows 原生工具）
│  Remote Web Server     │     暴露 /data.json
│  (端口 8085)           │
└──────────┬───────────┘
           │ 每 5 秒轮询
┌──────────▼───────────┐
│  lhm_alert_agent.py   │  ← 独立脚本，不依赖 FastAPI
│  - fetch_temperature()│     读取传感器温度
│  - 连续超阈 60s 触发  │
│  - 冷却 120s          │
│  - 携带最近 60 个数据点│
└──────────┬───────────┘
           │ POST /api/alerts/ingest
           │ Bearer ALERT_WEBHOOK_TOKEN
┌──────────▼───────────┐
│  AlertService         │  ← FastAPI 内部
│  - SHA-1 去重         │     host+sensor 生成 alert_id
│  - 首次告警发邮件      │     asyncio.create_task (fire-and-forget)
│  - critical + auto_   │
│    diagnosis 开关      │
│    → AIOps 自动诊断    │     asyncio.create_task
│    → 诊断邮件          │     含完整报告
│  - 内存 Active 列表    │     GET /api/alerts/active
└──────────────────────┘
```

**关键设计决策：**

- **轮询与主服务分离**：`lhm_alert_agent.py` 是独立脚本（不是 FastAPI 的一部分），纯标准库+requests，无 LangChain 依赖。这意味着硬件监控挂了不影响对话服务，反之亦然。
- **Webhook 解耦**：告警 Agent 通过 HTTP 上报，不直接操作 FastAPI 内部状态。好处是告警源可以任意扩展 —— 任何能发 HTTP POST 的监控工具都能接入。
- **去重机制**：`alert_id = SHA-1(host|sensor_id)`，出现过的 active 告警不会重复创建、重复发邮件。
- **异步邮件**：`asyncio.create_task()` 发送，不阻塞 API 响应。
- **冷却窗口**：`ONCALL_TEMP_COOLDOWN_SEC=120`，避免温度在阈值附近抖动时频繁告警（告警风暴）。

### Q16: 告警自动诊断是怎么触发的？怎么防止误触发？

**答：**

**触发条件（同时满足）：**
1. 告警 severity 为 `critical`（比 warning 高一级）。
2. `ONCALL_AUTO_DIAGNOSIS=true`（`.env` 配置，可随时关闭）。
3. 告警通过去重检查（同一 alert_id 不重复诊断）。

**防误触发措施：**
- **连续超阈时长**（`ONCALL_TEMP_DURATION_SEC=60`）：不是瞬间超过就告警，而是要持续超过阈值 60 秒才触发。过滤掉瞬时毛刺。
- **冷却窗口**（`ONCALL_TEMP_COOLDOWN_SEC=120`）：发送一次告警后，至少冷却 120 秒才允许下一次。防止在阈值附近抖动造成"告警轰炸"。
- **去重**：同一个 `alert_id` 不会重复触发诊断。
- **临界值分层**：温度超过阈值但低于 `threshold+5` 为 `warning`（不发诊断），超过 `threshold+5` 为 `critical`（触发诊断）。

---

## 五、本地日志检索与取证

### Q17: `search_log` 工具的设计思路是什么？为什么不直接用 ELK/CLS？

**答：**

`search_log` 是 LangChain `@tool`，从 `logs/app_*.log` 中按时间和关键词检索运行日志。

**实现要点：**
- 用正则解析结构化日志行：`YYYY-MM-DD HH:MM:SS | LEVEL | location | message`
- 支持多关键词 AND 匹配（所有关键词必须命中）
- 时间窗口（向前 N 分钟）+ 硬上限 500 条
- 返回结构化 dict，便于 LLM 理解

**为什么做本地日志检索，而不是完全依赖 ELK/CLS：**
1. **Agent 决策闭环**：诊断时 Agent 需要自主查询证据，而不是等人类切到 ELK 界面手动查。`search_log` 让 LLM 能像调用 `retrieve_knowledge` 一样自行查日志。
2. **离线可用**：不依赖外部日志平台，开发/测试环境（无 CLS 权限）也能跑完整诊断。
3. **降低延迟**：本地查文件比跨网络查 CLS API 快。
4. **分层互补**：本地日志覆盖应用层，CLS MCP（可选扩展）覆盖云基础设施层，两者互补而非替代。项目 Makefile 已预留 `start-cls` 目标。

### Q18 (八股): 日志系统设计，结构化日志 vs 非结构化日志？

**答：**

**非结构化日志**：`print("Error: user 123 failed to login")`
- 问题：难以机器解析、无法做字段级检索、聚合统计困难。

**结构化日志**：本项目使用 `loguru`，产出格式 `2025-01-01 12:00:00 | INFO | app.services.xxx | message`

- 用统一格式分隔 timestamp、level、location、message。
- `search_log` 用正则就能精确解析，支持按 level 过滤、按时间范围筛选。
- 方便后续接入 ELK/CLS 等日志平台（结构化数据自动建立索引字段）。

**补充：日志最佳实践**
- 用不同 level 区分重要程度（DEBUG < INFO < WARNING < ERROR < CRITICAL）。
- 关键操作记录 trace_id/session_id 便于链路追踪。
- 敏感信息脱敏后再打日志。

---

## 六、分层记忆（Hot/Cold Memory）

### Q19: 项目的分层记忆是怎么设计的？Hot 和 Cold 各存什么？

**答：**

```
memory/
├── MEMORY.md           ← Hot Memory（热记忆索引）
├── incidents/          ← Cold Memory（冷记忆完整报告）
│   ├── 20260101_120000_abc123.md
│   └── 20260102_083000_def456.md
└── artifacts/          ← Cold Memory（工具原始输出）
    └── abc123/
        ├── step1_20260101_120100.txt
        └── step3_20260101_120300.txt
```

**Hot Memory（`memory/MEMORY.md`）：**
- 一个 Markdown 表格，每行一条历史诊断事件。
- 包含：时间戳、session_id、启发式摘要（从报告中提取前 220 字符）、到 Cold 报告的链接。
- 限制最多 20 条，超出删旧条目。
- **每次 Planner 启动和 RAG Agent 对话时都会加载** → 给 LLM 提供"最近发生了什么"的上下文。

**Cold Memory（`memory/incidents/` + `memory/artifacts/`）：**
- `incidents/`：每次 AIOps 诊断的完整报告，含诊断步骤、中间结果和最终结论。
- `artifacts/`：Microcompact 落地的大工具结果原文，作为举证链条保留。
- **不会主动加载到 LLM 上下文** —— 只在需要回溯时由人类查看，或未来可支持 Agent 按需读取。

**设计意图：**

| | Hot | Cold |
|------|-----|------|
| 大小 | ~4KB（截断） | 无限制 |
| 加载频率 | 每次请求 | 仅回溯 |
| 作用 | 快速上下文 | 详细证据 |
| 类比 | CPU L1 Cache | 磁盘存储 |

### Q20 (八股): Agent 记忆有哪些类型？各自解决什么问题？

**答：**

Agent 记忆一般分三类：

**1. 短期记忆（Short-term Memory / Working Memory）：**

- 当前对话的上下文窗口内的消息历史。
- 问题：上下文窗口有限（虽然现在很多模型支持 128K/1M，但要考虑成本、延迟和注意力衰减）。
- 本项目处理：RAG Agent 用 **Snip 截断**（Level 1 压缩）—— 超阈值时保留 SystemMessage + 最近 6 条消息。

**2. 长期记忆（Long-term Memory）：**
- 跨会话的持久化记忆，让 Agent 能"记住之前的交互"。
- 本项目的 Hot/Cold Memory 就是一种长期记忆实现：
  - Hot：最近事件索引，快速注入上下文。
  - Cold：完整事件报告，需要时再深入查看。
- 其他常见方案：向量化历史对话存入向量库、知识图谱、用户 Profile 等。

**3. 工作流状态记忆（Checkpoint / State）：**
- LangGraph 的 `MemorySaver`，持久化 StateGraph 的执行状态（`past_steps`、`plan`、`response`）。
- 作用：支持中断恢复、会话隔离、审计回溯。

---

## 七、上下文压缩（Microcompact + Collapse + Snip）

### Q21: 项目的四层上下文压缩分别是什么？为什么需要多层压缩？

**答：**

Plan-Execute-Replanner 循环中，`past_steps` 会不断增长（每一步追加一条工具结果），很容易超出 LLM 上下文窗口。四层压缩分别是：

| 层级 | 名称 | 位置 | 触发条件 | 机制 |
|------|------|------|----------|------|
| Level 1 | Snip | RAG Agent | 消息历史 > 300 tokens | 截断旧消息，保留最近 6 条 |
| Level 2 | Microcompact | Executor → Replanner 之间 | 最新工具结果 > 100 tokens | 大结果落盘 `artifacts/`，替换为短摘要 + 路径 |
| Level 3 | Collapse | Replanner 内部 | `past_steps` 步骤数 > 1 | 旧步骤折叠为单行摘要，仅保留最近 2 步详情 |
| Level 4 | Autocompact | Replanner 之后 | `past_steps` 总 tokens > 300 | 监控统计，记录压缩日志 |

**为什么需要多层：**
- 单一策略无法覆盖所有场景。Level 2 针对单步大结果（如 500 行日志），Level 3 针对步骤累积过多。
- **分层防御**：即使某层漏过了（比如 Microcompact 阈值设高了），Collapse 也会在 Replanner 兜底。
- **信息保真度逐层递降**：Microcompact 保存原文到磁盘，Collapse 只保留摘要 —— 越早的步骤保留的信息越少，和人的认知规律一致（最近的信息最重要）。

### Q22: Microcompact 把原文写入文件后怎么引用？LLM 能理解吗？

**答：**

Microcompact 流程：
1. 检测 `past_steps[-1]` 的 tool result 长度 > 100 tokens。
2. 用 `aiofiles` 异步写入 `memory/artifacts/{session_id}/{step_index}_{timestamp}.txt`。
3. 把 `past_steps` 最后一步的 result 替换为：

```
[工具结果已压缩] 原始内容（共 {tokens} tokens）已保存至：
memory/artifacts/{session_id}/step{idx}_{ts}.txt
摘要：{前 300 字符}
```

LLM 在看到这个摘要后：
- 知道"有一个完整的工具结果在某个文件里"，未来如果需要可以告诉用户去查看。
- 通过前 300 字符的摘要理解大概内容，足够支撑后续决策。

这是一种**信息换空间**的策略 —— 上下文窗口内保留元信息，详细证据存磁盘，需要时再检索。

### Q23 (八股): 大模型上下文窗口越来越大（128K/1M tokens），还需要做压缩吗？

**答：**

**仍然需要。** 原因：

1. **成本**：大窗口 = 更多输入 tokens = 更高 API 费用。即使模型支持 1M 窗口，每次调用的 token 消耗也是线性增长的。
2. **延迟**：更多 tokens = 更长首 token 延迟（prefill 时间）。诊断场景用户等待时间不宜过长。
3. **注意力衰减**：模型对上下文中间和前面的信息关注度下降（"Lost in the Middle" 现象）。把关键信息压缩到更短的上下文反而提升理解质量。
4. **信息密度**：上下文窗口应该放高质量信息，而非冗余日志。压缩本质上是把低密度信息转化为高密度信息。
5. **多 Agent 协作**：如果未来需要多个 Agent 传递上下文，压缩后的结构化信息更容易在 Agent 间传递和组合。

总结：**窗口变大不等于可以不做压缩。压缩的目标不是"装得下"，而是"看得准 + 成本低"。**

---

## 八、LangGraph StateGraph 与 Agent 编排

### Q24: 为什么 Agent 编排图是从 Planner 开始而不是从 Executor 开始？

**答：**

这是 Plan-Execute-Replanner 模式的设计原则：

1. **先规划后执行**：Planner 先审视全局（用户需求、历史记忆、知识库、可用工具），制定分步计划。没有计划直接执行，Agent 容易"走一步看一步"，缺乏全局视角，容易跑偏。
2. **可解释性**：计划显式呈现给用户（SSE 事件 `plan`），用户能看到 Agent "准备怎么做"，而不是黑盒操作。
3. **Replanner 的锚点**：后续 Replanner 判断"当前计划是否合理"、"是否需要重新规划"时，需要一个原始计划作为参照基准。
4. **Skill 注入时机**：Skill 描述在 Planner 阶段注入，影响计划生成 —— 如果匹配到"CPU 高负载诊断" Skill，计划会遵循其推荐步骤。

### Q25 (八股): Tool Calling 的原理是什么？LLM 怎么知道该调用哪个工具？

**答：**

**Function Calling 的机制：**

1. **工具声明**：把工具定义（name、description、parameters JSON Schema）随请求一起发送给 LLM。
2. **模型判断**：LLM 看到用户输入 + 工具列表后，如果判断需要查外部信息，就生成一个 tool_call（含工具名和参数 JSON），而不是直接生成文本回复。
3. **执行工具**：调用方（LangChain/LangGraph）收到 tool_call 后，执行对应的 Python 函数，拿到结果。
4. **结果回传**：把 tool_call + tool_result 作为新消息追加到对话中，LLM 根据工具结果生成最终回复。

**LLM 怎么判断调用哪个工具：**
- 训练时模型见过大量 tool calling 样本，学会了根据用户意图和工具 description 匹配。
- 工具 description 写得好坏直接影响调用准确率 —— 要写清楚"什么时候用、参数是什么、返回什么"。

**本项目中的实现：**
- `llm.bind_tools(all_tools)` → LangChain 自动把 tool 转为 OpenAI 兼容的 tool schema。
- `ToolNode` → LangGraph 提供的自动工具执行节点，它检测 LLM 输出的 tool_calls 并执行。
- 工具调用后，把结果再喂给 LLM 做最终合成（即 ReAct 模式：Reasoning + Acting 交替）。

---

## 九、Skill 系统

### Q26: 项目中 Skill 的结构是什么？它是怎么注入到诊断流程中的？

**答：**

**Skill 文件格式**（`.claude/skills/*.md`）：

```markdown
---
id: CPUHighLoadDiagnosis
name: 高 CPU 负载诊断
description: 针对 CPU 使用率异常升高场景的标准化排查流程
---

# 高 CPU 负载诊断 Skill

适用于：CPU 飙高、某进程占用 CPU 过高等场景。

## 推荐步骤
1. 使用 Monitor MCP 查询 CPU 使用率最高的进程
2. 使用 search_log 查询异常日志
3. 综合判断根因并给出处理建议

## 规划时请遵守
- 制定计划时可带上 Skill 标识 `CPUHighLoadDiagnosis`
- 每一步应写明拟调用的工具名称及必要参数思路
```

**结构解析：**
- **YAML frontmatter**（`---` 分隔）：`id`（唯一标识）、`name`（可读名）、`description`（简短描述）。
- **正文 Markdown**：排查步骤、注意事项、规划约束。最长 3500 字符。

**注入流程：**
1. `load_skills_description()` 扫描 `.claude/skills/*.md`。
2. 解析 YAML frontmatter + 截断正文（≤ 3500 字符）。
3. 格式化为 Markdown 块，注入 Planner 的 System Prompt：

```
### Skill: `CPUHighLoadDiagnosis`
**名称**: 高 CPU 负载诊断
**描述**: 针对 CPU 使用率异常升高场景的标准化排查流程
**正文**:
... (排查步骤详情)
```

4. Planner 看到 Skill 列表后，在制定计划时可以引用 Skill ID，使步骤与标准化流程对齐。

**Skill vs Prompt 的区别：**
- Prompt 是通用的（"你是一个运维专家"）。
- Skill 是专用的、可插拔的知识单元，按场景组织。新增场景只需加一个 `.md` 文件，不需要改代码。

### Q27 (八股): 设计一个通用的 Skill/Plugin 系统，需要考虑哪些要素？

**答：**

1. **注册与发现**：Skill 如何被系统感知？文件扫描（本项目方式）、数据库存储、远程注册中心？
2. **元数据格式**：id、name、description 是必需的，是否还需要 version、author、dependencies、适用模型？
3. **触发方式**：被动注入 Prompt（本项目）、主动匹配（Intent 路由）、用户显式指定？
4. **作用域**：Skill 影响哪个环节？Planner（计划生成）、Executor（工具选择）、Replanner（结论格式）？
5. **隔离与安全**：Skill 是否可以执行任意代码？sandbox 机制？权限控制？
6. **冲突处理**：多个 Skill 匹配时，优先级？合并还是互斥？
7. **生命周期**：加载时机（启动/按需）、热加载（不重启更新 Skill）、版本管理。
8. **效果评估**：Skill 是否真的提升了诊断质量？A/B 测试、用户反馈。

---

## 十、系统架构与设计模式

### Q28: 项目中用了哪些设计模式？分别在哪里？

**答：**

| 设计模式 | 应用位置 | 说明 |
|----------|----------|------|
| **Singleton** | `config`、`aiops_service`、`rag_agent_service`、`alert_service`、`memory_writer`、`mcp_client`、`vector_store_manager` | 模块级全局实例，确保配置/服务/客户端唯一 |
| **Chain of Responsibility** | `retry_interceptor` → MCP tool handler | 拦截器链包装 MCP 工具调用，每个拦截器可决定继续、重试或返回错误 |
| **Observer** | SSE 流式推送、`TokenMeter` 日志监控 | 诊断过程每一步产生事件，前端和后端监控都能响应 |
| **Strategy** | Planner 中 Skill 注入、Executor 中本地/MCP 工具选择 | 不同工具来源（本地 vs MCP）是不同策略，统一 `bind_tools()` 接口 |
| **Template Method** | `execute()` / `diagnose()` / `execute_alert_diagnosis()` | 后者是前者的模板化变体，核心流程（execute）不变，只是 prompt 不同 |
| **Decorator** | `@tool`、`@mcp.tool()`、`@log_tool_call` | 声明式定义工具，框架自动处理参数解析、错误处理、日志记录 |
| **Facade** | `AIOpsService` | 对外暴露 `execute()`/`diagnose()` 简单接口，内部封装 LangGraph 编译、流式执行、记忆落盘等复杂性 |
| **Adapter** | `MultiServerMCPClient` | 把 MCP 协议的工具适配为 LangChain BaseTool |
| **Producer-Consumer** | `asyncio.create_task()` 邮件/诊断 | 告警接收（Producer）立即返回，邮件和诊断（Consumer）异步后台执行 |

### Q29: 如果要把这个系统部署到生产环境，还有哪些需要改进的？

**答：**

1. **状态持久化**：当前 LangGraph 用 `MemorySaver`（内存），进程重启后诊断状态丢失。生产应换 `SqliteSaver` 或 `PostgresSaver`。
2. **告警存储**：告警 Active 列表在内存中，重启丢失。应接入 Redis 或数据库做持久化。
3. **并发与扩展**：`AIOpsService` 是单例，多用户同时诊断时共享 LLM 和 MCP 客户端。需要增加并发控制（如信号量、请求队列），避免同时发起过多 LLM 调用。
4. **认证鉴权**：当前只有告警 Webhook 有 Bearer 认证，对话和诊断接口无鉴权。生产需加 API Key 或 OAuth。
5. **监控与可观测性**：增加 Prometheus metrics（请求量、延迟、错误率、token 消耗）、分布式追踪（trace_id 贯穿 Planner→Executor→Replanner）。
6. **MCP 高可用**：当前 MCP 是单点，生产可部署多个 MCP Server 副本 + 负载均衡。
7. **LLM 限流与降级**：增加 rate limiter 和 circuit breaker，LLM 不可用时返回降级回复而非报错。
8. **Skill 热加载**：当前 Skill 启动时加载一次，生产应支持热更新（文件监听或 API 刷新）。

### Q30: 为什么用 OpenAI 兼容 API 而不是直接调某个特定模型的 SDK？

**答：**

项目通过 `DASHSCOPE_API_BASE` + `DASHSCOPE_API_KEY` 配置 LLM，底层是 `langchain-openai` 的 `ChatOpenAI`。设计意图：

1. **模型无关性**：OpenAI 兼容 API 已经成为事实标准，DeepSeek、DashScope、vLLM、Ollama 等都提供兼容接口。改一个 URL 就能换模型，不需要改代码。
2. **成本灵活**：开发调试用便宜的 DeepSeek，生产可切到更强的模型，甚至按场景路由不同模型（简单问答用便宜模型，复杂诊断用强模型）。
3. **避免供应商锁定**：不绑定任何一个模型厂商。
4. **LangChain 生态**：`langchain-openai` 的 `ChatOpenAI` 是一等公民，`bind_tools()`、`with_structured_output()` 等高级功能都最稳定可靠。

---

## 十一、Python / 异步编程

### Q31: 项目中大量使用了 async/await，为什么？哪些地方用同步就够了？

**答：**

**异步的使用场景：**
- FastAPI 路由处理（`async def` endpoint）—— 不阻塞事件循环，能并发处理多个请求。
- SSE 流式输出 —— `graph.astream()` 异步流式产生事件，边生成边推送。
- MCP 工具调用 —— `MultiServerMCPClient` 底层用 `httpx` 异步 HTTP。
- 文件写入（Microcompact）—— `aiofiles` 不阻塞事件循环。
- 邮件/诊断 —— `asyncio.create_task()` 异步后台执行。

**同步的地方：**
- `lhm_alert_agent.py` —— 独立脚本，简单 while True 循环，同步 `requests` 足够。
- 本地 `search_log` —— 读本地小日志文件，I/O 很快，同步 `open()` 读取即可。
- `retrieve_knowledge` —— 虽然 LangChain 推荐 `ainvoke()`，但内部 Milvus 查询也是 I/O，如果并发不高同步也行。

**判断标准：** 关键路径上需要并发的用 async，独立脚本/低频调用/简单逻辑用同步即可，避免为了 async 而 async。

### Q32 (八股): asyncio.create_task、await、gather 的区别？

**答：**

- **`await`**：等一个协程执行完成，拿返回值。期间当前协程挂起，事件循环可以调度其他任务。是"串行等"。
- **`asyncio.create_task()`**：把协程包装成 Task 提交到事件循环后台执行，不阻塞当前协程。返回一个 Task 对象，之后可以 `await task` 拿结果。是"后台跑"。
- **`asyncio.gather()`**：同时等多个协程/Task 完成，返回结果列表。是"并行等"。

本项目中的使用：
```python
# AlertService: 不阻塞 API 响应，后台发邮件
asyncio.create_task(self._send_alert_email(record))

# AIOps: 异步流式逐个处理事件
async for event in execute():
    yield event

# 如果多个独立查询，可以用 gather 并发
results = await asyncio.gather(
    query_cpu(), query_memory(), search_log()
)
```

---

## 十二、候选追问速答

### Q33: Pydantic Settings 的 `case_sensitive=False` 有什么意义？

环境变量名不区分大小写，`MILVUS_HOST` 和 `milvus_host` 等效。用户在不同操作系统（Windows 环境变量习惯全大写，Linux 可能小写）下都不出问题。

### Q34: 为什么 `past_steps` 存的是 `List[tuple]` 而不是 `List[dict]`？

Tuple 是 immutable，在 LangGraph state 更新时不会出现引用共享问题；dict 是 mutable，可能在 reducer 之前就被意外修改。

### Q35: MCP 的 `streamable-http` 和 `stdio` transport 怎么选？

- `streamable-http`：适合独立部署的服务（本项目 Monitor MCP），可跨机器调用。
- `stdio`：适合与 LLM 进程同机的工具，启动子进程通信，延迟更低但需要进程管理。

### Q36: 文档切分时为什么提取 h1/h2/h3 到 metadata？

检索结果的上下文不仅是 chunk 本身，metadata 中的标题层级能帮助 LLM 理解这个 chunk 来自哪个文档的哪个章节，生成答案时可以引用出处、避免张冠李戴。

### Q37: `structured_output` 的 `method="function_calling"` 原理？

LangChain 把 Pydantic 模型转为 JSON Schema，传给 LLM 的 tool_choice，LLM 按要求输出符合 Schema 的 JSON，LangChain 再反序列化为 Pydantic 实例。本质上是利用了 function calling 来约束输出格式。

---

## 十三、Claude Code 的分层记忆设计

> 本项目的 Hot/Cold Memory 设计受 Claude Code 的记忆系统启发。以下是 Claude Code 内部的三层记忆架构及其设计哲学。

### Q38: Claude Code 的三层记忆是怎么划分的？各自存什么、何时加载？

**答：**

Claude Code 把记忆按"温度"分为三层，不同温度用不同策略管理：

**第一层 — 热数据：`MEMORY.md`（常驻加载）**

`MEMORY.md` 就像一本书的目录，每次对话启动时都会加载到上下文里。但它不存内容，只存指针（文件名 + 一行描述），指向真正的话题文件。

两层限制确保它不会撑爆上下文窗口：
- **行数限制**：最多 200 行，每行约 150 字符
- **字节限制**：最多 25KB（行数短但内容长也能兜底）

字节限制是后来补加的。统计发现，有些用户虽然没超 200 行，但每行写了一大堆，200 行能塞到 197KB。不加字节兜底的话，上下文窗口一大半全被索引占了。

截断逻辑也做得细致——先按行截断，再按字节在最后一个 `\n` 处切开（不会切到一行的中间），最后追加一条 WARNING 告诉模型"索引没加载完整"：

```
> WARNING: MEMORY.md is truncated. Only part of it was loaded.
```

**第二层 — 温数据：话题文件（按需加载）**

编码偏好、架构约定、踩过的坑这些细节存在单独的话题文件（如 `user_role.md`、`feedback_testing.md`），并不常驻。

新对话开始时，用一个小模型（如 Sonnet）来挑最多 **5 个**与当前对话最相关的话题文件加载进来。选择规则的设计也很务实——如果某个工具正在被频繁使用，就**不加载它的使用文档**（你都用上了说明你会用），但**一定要加载它的已知问题和坑**（正在用的时候最怕踩坑）。

另外，Claude Code 的记忆**不记代码事实**。函数在第几行、API 叫什么名、配置项怎么写——这些永远通过 `Grep`、`Glob`、`Read` 去源码里实时查，不写进记忆。因为代码会重构，而记忆不会自动更新。如果记忆里写着"函数 X 在 30 行"，重构后挪到 60 行了，这记忆就变成误导。缓存与数据不一致是后端系统常见的坑，Claude Code 从根源上消灭了这类问题。

**第三层 — 冷数据：历史对话（Grep 搜索）**

更早的历史对话存为 `.jsonl` 文件，需要时用 `Grep` 按关键词搜索就能找到。不占用上下文窗口，只在明确需要时才进入视野。

**三层对比：**

| 层级 | 存储形式 | 加载策略 | 类比 |
|------|----------|----------|------|
| 热 | `MEMORY.md` 索引 | 每次都加载 | CPU L1 Cache |
| 温 | 话题 `.md` 文件 | 小模型按需选 ≤5 个 | CPU L2/L3 Cache |
| 冷 | 历史 `.jsonl` | Grep 搜索 | 磁盘 |

**本项目对齐情况：**

| 本项目 | Claude Code 对应 | 差异 |
|--------|-----------------|------|
| `memory/MEMORY.md` 表格 | `MEMORY.md` 索引 | 本项目存表格（摘要内联），Claude Code 只存指针 |
| `memory/incidents/` | 话题文件 | 本项目不自动按需加载，需手动回溯 |
| `memory/artifacts/` | 无直接对应 | 工具原文证据 |

**启发与改进方向：**
- 本项目 Hot Memory 目前每次全量加载，可以加截断上限和 WARNING 提示。
- 可以引入"温检索"机制——用小模型按语义从 incidents/ 中挑最相关的报告注入 Planner，而非只加载表格索引。
- 严格区分"项目事实"（存代码里）和"运维经验"（存记忆里），不把代码路径/配置值写进 MEMORY.md。

---

## 十四、Agent 上下文压缩（五级漏斗）

> 本项目的四层压缩（Snip / Microcompact / Collapse / Autocompact）借鉴了 Claude Code 内部的五级压缩架构。以下是 Claude Code 的全套方案。

### Q39: 为什么要做上下文压缩？Claude Code 的五级压缩分别是什么？

**答：**

大模型上下文窗口有上限（即使 1M tokens，成本、延迟和注意力衰减仍是问题）。随着对话越来越长、工具调用结果越攒越多，token 用量会快速膨胀。压缩的目标不是"装得下"，而是让有限窗口里装的信息密度尽量高。

Claude Code 设计了五级压缩，像一个漏斗层层过滤，从轻到重依次触发：

| 级别 | 名称 | 触发时机 | 机制 |
|------|------|----------|------|
| 1 | **Snip 剪裁** | 消息历史过长 | 旧工具调用结果只保留结构（tool_call_id + 状态标记），删除内容体 |
| 2 | **Microcompact 微压缩** | 单步工具结果过大 | 大结果卸载到缓存（注意是缓存而非直接丢弃，子智能体后续可能还要用） |
| 3 | **Context Collapse 折叠** | 上下文占用持续升高 | 对中间对话做折叠摘要，只保留关键信息，去掉冗余细节 |
| 4 | **Autocompact 自动压缩** | 上下文超过阈值 | 触发全量摘要压缩，把已有上下文整体压缩为简版 |
| 5 | **Reactive Compact 应急压缩** | API 返回 413 错误 | 最后兜底，紧急裁减上下文后重试 |

这五级通过 **Feature Flag** 按需引入：

```typescript
const reactiveCompact = feature('REACTIVE_COMPACT')
  ? require('./services/compact/reactiveCompact.js') : null
const contextCollapse = feature('CONTEXT_COLLAPSE')
  ? require('./services/compact/contextCollapse.js') : null
const snipModule = feature('HISTORY_SNIP')
  ? require('./services/compact/snipCompact.js') : null
```

### Q40: 压缩机制中有哪些让人印象深刻的设计细节？

**答：**

**1. 断路器（Circuit Breaker）保护**

在 `autoCompact.ts` 中有一段注释非常说明问题：

```
// Stop trying autocompact after this many consecutive failures.
// BQ 2026-03-10: 1,279 sessions had 50+ consecutive failures
// (up to 3,272) in a single session, wasting ~250K API calls/day globally.
const MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES = 3
```

2026 年 3 月 10 号统计发现，有 1279 个会话连续压缩失败了 50 次以上，最夸张的一个会话连续失败了 **3272 次**还在不停重试，全球每天浪费约 **25 万次 API 调用**。

所以加了断路器：连续失败 3 次就自动停下来，不再重试。这是一个典型的"线上事故驱动设计"——不管理论多完善，生产环境总会出现意料之外的情况，兜底机制要到位。

**2. 按需裁剪，能省则省**

五个级别的设计体现了"渐进式压缩"思想。能裁掉内容体的先裁（Snip，只去内容不去结构），实在不够再上更重的方案（Collapse 摘要，有信息损失），最后才用终极手段（Reactive Compact，应急保命）。每一层都试图用最小代价解决问题。

**3. Snip 保留结构**

Snip 不是暴力删消息，而是只去掉旧工具调用结果的 content，保留 tool_call_id 和完成状态标记。这样 Agent 知道"那个工具调过了，结果是啥已经不重要了"，但不会丢失"调过什么工具"的结构信息。把结构信息和内容信息分开对待，是一种很精细的设计。

**本项目对齐情况：**

| 本项目 | Claude Code 对应 | 差异 |
|--------|-----------------|------|
| Snip（Level 1） | Snip 剪裁 | 本项目保留最近 6 条非系统消息 |
| Microcompact（Level 2） | Microcompact 微压缩 | 本项目写文件而非缓存 |
| Collapse（Level 3） | Context Collapse 折叠 | 机制相似，旧步骤折叠为单行摘要 |
| Autocompact（Level 4） | Autocompact 自动压缩 | 本项目仅记录统计日志，未做全量摘要 |
| 无 | Reactive Compact 应急压缩 | 本项目缺少 413 兜底重试 |

**启发与改进方向：**
- 可以给本项目的 Autocompact 加上**断路器**，防止压缩失败时陷入死循环。
- 可以增加 Reactive Compact 作为最后一层兜底，在 LLM API 返回超长错误时紧急裁减后重试。
- Feature Flag 的引入方式值得借鉴——压缩模块按需加载，A/B 测试时可以灵活控制启用哪些层级。

---

## 十五、MCP Tool Schema 与调用机制

### Q41: Schema 是自动生成的吗？在哪里能看到？怎么做到自动生成的？

**答：**

是的，全自动。项目里没有任何 `.json` 或 `.yaml` 文件存放 MCP tool 的 schema，它是在运行时由 Pydantic 动态生成的。

**在哪能看到？**
1. Python 脚本：`mcp._tool_manager._tools.values()` 里每个 tool 的 `.parameters` 字段
2. curl 直接调 MCP 端点：`POST http://localhost:8004/mcp`，body 为 `{"jsonrpc":"2.0","id":1,"method":"tools/list"}`，返回的 `inputSchema` 就是
3. Client 侧：`await mcp_client.get_tools()` 后每个 tool 的 `.args_schema`

**怎么自动生成？** 核心链路：Python 函数 + 类型注解 → Pydantic `type_adapter.json_schema()` 生成 JSON Schema → MCP 协议传给客户端 → `langchain-mcp-adapters` 设到 `StructuredTool.args_schema` → `llm.bind_tools()` 转成 OpenAI function calling 格式发给 LLM。

---

### Q42: 用 `query_cpu_metrics` 举例，自动 Schema 生成全流程 + `@mcp.tool()` 源码内部 + Server/Client 最少代码

**以 `query_cpu_metrics` 为例的完整 7 步流程：**

```python
@mcp.tool()
def query_cpu_metrics(
    service_name: str,
    start_time: Optional[str] = None,
    end_time: Optional[str] = None,
    interval: str = "1m"
) -> Dict[str, Any]:
```

```
函数签名
  → @mcp.tool() 装饰器
  → ParsedFunction.from_function()
      ├─ inspect.signature(fn) → 拿到参数名、类型、默认值
      ├─ get_cached_typeadapter(fn).json_schema() → Pydantic 生成 JSON Schema
      └─ compress_schema() → 去掉 title、多余 $defs
  → Tool.parameters = {"type":"object","properties":{...},"required":[...]}
  → MCP 协议 tools/list → 客户端拿到 inputSchema
  → convert_mcp_tool_to_langchain_tool() → StructuredTool(args_schema=tool.inputSchema)
  → llm.bind_tools() → OpenAI function calling 格式 → 发给 LLM
```

**`@mcp.tool()` 源码内部（5 层）：**
1. `FastMCP.tool()` 装饰器入口 — 判断调用模式，转发给 `Tool.from_function()`，注册到 ToolManager
2. `Tool.from_function()` → `FunctionTool.from_function()` — 转发层
3. `FunctionTool.from_function()` — 调 `ParsedFunction.from_function()` 做类型自省，拿 input/output schema，实例化
4. `ParsedFunction.from_function()`（核心）— 禁止 `*args/**kwargs` → 提取函数名和 docstring → 处理依赖注入参数 → **Pydantic `type_adapter.json_schema()`** 生成 JSON Schema → `compress_schema()` 压缩 → 处理返回值 schema
5. `Tool.to_mcp_tool()` — 转成 MCP 标准格式 `MCPTool(name, description, inputSchema, outputSchema)`

**MCP Server 最少代码（≈15 行）：**

```python
from fastmcp import FastMCP

mcp = FastMCP("MyServer")

@mcp.tool()
def add(a: int, b: int) -> int:
    """两个数相加"""
    return a + b

if __name__ == "__main__":
    mcp.run(transport="streamable-http", host="127.0.0.1", port=8004, path="/mcp")
```

三步：创建 FastMCP → `@mcp.tool()` 装饰函数 → `mcp.run()` 启动。Schema 生成、工具注册、协议处理全自动。

**MCP Client 最少代码（≈20 行）：**

```python
import asyncio
from langchain_mcp_adapters.client import MultiServerMCPClient

async def main():
    client = MultiServerMCPClient({
        "my_server": {
            "transport": "streamable-http",
            "url": "http://localhost:8004/mcp",
        }
    })

    tools = await client.get_tools()
    result = await tools[0].ainvoke({"a": 1, "b": 2})

asyncio.run(main())
```

三步：配置 Server 地址 → `get_tools()` 拿工具 → `tool.ainvoke()` 调用。

---

### Q43: 这不就是自己写的函数参数吗？为什么说是自动生成？Schema 里为什么没有 CPU 占用率？

**答：**

**你写的是 Python 代码，不是 Schema。** 你写的：

```python
def query_cpu_metrics(service_name: str, start_time: Optional[str] = None, ...):
```

框架自动生成的是发给 LLM 的 JSON：

```json
{
  "type": "object",
  "properties": {
    "service_name": {"type": "string"},
    "start_time": {"anyOf": [{"type": "string"}, {"type": "null"}], "default": null}
  },
  "required": ["service_name"]
}
```

映射关系：`str` → `{"type":"string"}`、`Optional[str] = None` → `{"anyOf":[{"type":"string"},{"type":"null"}],"default":null}`、无默认值 → 自动进 `required`。Pydantic 读你的类型注解自动完成了这个转换。

**为什么 Schema 里没有 CPU 占用率？** Schema 分两种：

| | `inputSchema`（入参） | `outputSchema`（返回值） |
|---|---|---|
| 描述什么 | 调用时传什么参数 | 调用后返回什么 |
| 包含什么 | `service_name`, `start_time` 等 | CPU 占用率、平均值等 |

CPU 占用率是**出参**，不在 `inputSchema` 里。返回值 LLM 直接读文本，不需要 Schema 来校验。

---

### Q44: 从零开始，想调用一个 MCP 工具要做什么？

**答：**

需要写两段代码（Server + Client），一次完整调用的时间线：

```
1. Client 启动
2. Client → POST /mcp, body: {"method":"tools/list"}
   问："你有什么工具？"
3. Server 返回 tool 列表（含 inputSchema）
4. Client 将 tool 转成 LangChain StructuredTool
5. Client → POST /mcp, body: {"method":"tools/call",
           "params":{"name":"query_cpu","arguments":{...}}}
6. Server 收到 → type_adapter.validate_python(arguments) → 执行函数
7. Server 返回 {"content":[{"text":"..."}]}
```

| 你做的 | 框架自动完成的 |
|--------|--------------|
| Server: 函数 + 类型注解 + `@mcp.tool()` | Schema 生成、HTTP 协议、工具注册 |
| Client: 地址 + `get_tools()` + `ainvoke()` | 连接、tools/list、tools/call、格式转换 |

---

### Q45: MCP 工具和 LibreHardwareMonitor / psutil 是什么关系？

**答：**

**MCP 工具不实现业务逻辑，它只是一个壳/适配器。**

```
你要的             谁真正干活            MCP 工具干了什么
CPU 使用率         psutil（读 OS）      把 psutil 结果包装成 LLM 能看的 dict
内存使用率         psutil（读 OS）      同上
CPU 温度           LibreHardwareMonitor 发 HTTP 请求读 data.json，解析后返回
```

**LibreHardwareMonitor 需要提前手动配置：**
1. 下载 `LibreHardwareMonitor.exe`
2. 运行后开启 Remote Web Server，端口 8085
3. 此时 `http://127.0.0.1:8085/data.json` 才有数据
4. MCP 工具 `get_lhm_temperature()` 只是去请求这个 URL

psutil 不需要额外进程，`pip install psutil` 即可直接读系统 API。

**一句话**：MCP 工具 = 把已有数据源的能力包装成 LLM 可调用的接口。数据源得自己准备好，MCP 只负责"LLM 说人话 → 调底层工具 → 结果翻译回人话"这一段。

---

### Q46: "入参"是什么意思？

**答：**

入参 = **调用工具时传给工具的参数**。类比打电话："入参"是拨号时输入的号码，"返回值"是听到的回答。

```
入参    → inputSchema 描述，LLM 用它来传正确的东西进去
返回值  → LLM 直接读文本内容，不需要 Schema 提前定义结构
```

LLM 只负责**点菜**（传入参），菜上来了**吃了才知道什么味道**。

---

### Q47: 工具怎么接收我的入参？入参为什么这么设计？

**答：**

**参数传递机制：** 核心在 FastMCP `FunctionTool.run()`：

```python
async def run(self, arguments: dict[str, Any]) -> ToolResult:
    wrapper_fn = without_injected_parameters(self.fn)
    type_adapter = get_cached_typeadapter(wrapper_fn)
    result = type_adapter.validate_python(arguments)  # 🔥 dict → 函数调用
```

`type_adapter.validate_python(arguments)` 做了：读函数签名，把 dict 的 key 匹配到参数名 → 校验类型 → 没传的参数填默认值 → 真正调用函数。

**完整链路：**

```
LLM 输出 {"name":"query_cpu_metrics","arguments":{"service_name":"x"}}
  → ToolNode 调 tool.ainvoke({"service_name":"x"})
  → langchain-mcp-adapters 发 JSON-RPC 到 MCP Server
  → FastMCP FunctionTool.run(arguments={"service_name":"x"})
  → Pydantic type_adapter.validate_python({"service_name":"x"})
  → query_cpu_metrics(service_name="x") 执行
```

**参数设计理由：**

| 参数 | 设计 | 理由 |
|------|------|------|
| `service_name: str` | 必填，不设默认值 | 诊断一定针对某服务；为将来接 Prometheus 等真实监控预留 |
| `start_time/end_time: Optional[str] = None` | 可选字符串 | LLM 不一定会指定时间，默认最近一小时；用 `str` 而非时间戳因为 LLM 更擅长生成可读时间 |
| `interval: str = "1m"` | 有默认值 | 大部分场景 1 分钟粒度够用，省 LLM 心智 |

**设计原则：** 必填放前面强制 LLM 思考 → 常用场景给默认值 → LLM 友好的类型 → 预留扩展点。

---

### Q48: 为什么是 Client 发请求，不应该是 Server 发吗？

**答：**

因为 MCP 用的是**请求-响应模型**，不是推送模型。Server 只响应，不主动。

```
❌ 你以为的：MCP Server 检测到 CPU 高了 → 主动推送数据给 Client
✅ 实际的：LLM 说"查一下 CPU" → Client 去问 MCP Server → Server 返回结果
```

本项目的两层 Client-Server：

| 角色 | 端口 | 身份 | 主动/被动 |
|------|------|------|----------|
| `main.py` (FastAPI) | 9900 | HTTP Server + MCP Client | 被动等用户 + **主动调** MCP |
| `monitor_server.py` | 8004 | MCP Server | **纯被动**，等 Client 来调 |
| LibreHardwareMonitor | 8085 | HTTP Server | **纯被动**，等 MCP 工具来读 |

MCP Server 不是"监控系统推数据"的那种 Server，而是"提供工具等待被调用"的 Server。

---

### Q49: `MultiServerMCPClient` 是自带的吗？干什么的？

**答：**

是 `langchain-mcp-adapters` 自带的，`pip install langchain-mcp-adapters` 就有。

它是**翻译官 + 信使**，封装了所有 MCP 协议的脏活：

```
你调用                              MultiServerMCPClient 内部帮你做的
client = MultiServerMCPClient({...})  存好 Server 地址
client.get_tools()                    ① POST /mcp → tools/list
                                      ② 把返回的 tool 转成 LangChain StructuredTool
tool.ainvoke({...})                   ① POST /mcp → tools/call
                                      ② 把返回结果转成可用格式
```

不用它就得自己手写 HTTP 请求、拼 JSON-RPC、手动转 LangChain 格式。**核心就两个方法：`get_tools()` 拿工具列表，`tool.ainvoke()` 调工具。**

---

## 十五（增）. Agent 评估体系 (Eval Harness)

> 2026-05-19 新增：基于 ground truth 的 RAG 检索评测 + LLM-as-Judge 诊断报告评分。

### Q50: 你怎么评估 AIOps 诊断质量？准确率多少？

**答：**

两套量化指标，分别测 RAG 检索和 LLM 输出。

**检索端（纯向量计算）：**
- 8 个合成崩溃场景，每场景标注 `relevant_docs`（应召回的文档列表）
- 用 `vector_store_manager.similarity_search()` 做 top-5 检索
- 计算 Recall@5 / Precision@5 / MRR 三个指标
- 跑一次 8 场景约 2 秒，不调 LLM

**生成端（LLM-as-Judge）：**
- 结构评分量规：根因准确性(0-4) + 证据引用(0-3) + 建议可操作性(0-3) = 10 分制
- 用 `with_structured_output(EvalScore, method="function_calling")` 返回固定 JSON
- temperature=0 保证同一份报告跑两次分数可复现
- 每次跑完存 `.last_eval_score`，下次自动对比分数趋势

### Q51: 介绍下你的 eval 场景是怎么设计的？

**答：**

8 个场景覆盖我的实际崩溃类型，每个 `.json` 文件三个关键字段：

1. **`alert`** — 虚拟告警 payload，模拟 lhm_alert_agent 通过 Webhook 上报的内容
2. **`expected`** — `must_mention`/`should_mention`/`must_not_mention`，给 LLM-as-Judge 做对照
3. **`relevant_docs`** — 标注这个场景应该从知识库搜到哪几篇文档，给 RAG 评测做 ground truth

场景覆盖：栈溢出 / Access Violation / OOM / MEX 崩溃 / 内存泄漏 / CPU 高负载（伪告警）/ 未知退出 / 手动关闭（伪告警）。

### Q52: LLM-as-Judge 怎么看你的报告？你的判断标准有效吗？

**答：**

Judge 拿到两个东西：场景的 `expected` 期望关键词，和诊断报告全文。按三个子维度分别打分：

- **根因准确性(0-4)**：是否命中 `must_mention`？有没有出现 `must_not_mention`？没命中核心结论扣分。
- **证据引用(0-3)**：是否引用了调用栈、系统快照、日志？空泛说辞不给分。
- **建议可操作性(0-3)**："建议排查"得 0 分。"用迭代替代递归，加 dbstack 深度检查"得 3 分。

温度 0 + Structured Output 保证可复现。不是为了"绝对精确"，而是为了**有基线可对比**——改完 prompt 跑一次，分数涨了就是改对了。

### Q53: Recall@K / Precision@K / MRR 在你项目里的实际值？

**答：**

| 指标 | 值 | 解读 |
|------|:--:|------|
| Recall@5 | 82% | 该搜到的文档基本能搜到，覆盖面可以 |
| Precision@5 | 55% | 返回值里一半是噪声，chunk 可能太碎 |
| MRR | 89% | 第一个相关文档排名很靠前，排序质量好 |

Precision 偏低说明知识库里有相似但无关的文档混入结果。改进方向：调 chunk_size 减少碎片化，或加 Reranker 做精排。

### Q54: 这套 eval 的实际价值是什么？面试官凭什么觉得你做出了工程差异？

**答：**

不是一个"炫技"功能，而是解决了 Agent 开发的核心痛点——**调 prompt 靠感觉**。

之前改完 `replanner_prompt` 或者调了 chunk_size，只能靠人眼看一两篇报告"感觉好像好了一点"。现在跑 `make eval`，两张表出来，检索和生成两端都有量化对比。

面试官问"你怎么知道现在比之前好"，你能直接给出数字。这就是区别。
