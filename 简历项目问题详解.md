# SuperBizAgent 简历项目问题详解

本文完全按照当前仓库代码解释，不把设计设想当成已经实现的能力。适合用于面试前复习，也可用来校正简历表述。

## 一、先给出整个系统的真实调用链

```text
start-windows.bat
  ├─ 启动 Milvus
  ├─ 启动 LibreHardwareMonitor
  ├─ 启动 MCP Monitor Server
  ├─ 启动 FastAPI
  └─ 最小化启动 scripts/lhm_alert_agent.py
          │
          ├─ 每 5 秒查询 MATLAB.exe 是否存在
          ├─ 每 5 秒采集 CPU、内存、Top 进程
          ├─ 温度监控开启时查询 LHM Web API
          ├─ 保存 last_snapshot.json，并向 FastAPI 发送心跳
          │
          └─ 上轮存在、本轮消失
                  ├─ 查找最新 crash dump
                  ├─ 解析崩溃类型和调用栈
                  ├─ 采集当前系统快照
                  └─ POST /api/alerts/ingest
                          ├─ 告警去重
                          ├─ 后台发送首次告警邮件
                          └─ critical 告警触发 LangGraph 诊断
                                  Planner → Executor → Microcompact → Replanner
```

这里应当区分三个概念：

- **主动轮询 Agent**：常驻的 Python 监控脚本。
- **Webhook/FastAPI**：监控脚本与服务端之间的事件通知接口。
- **LangGraph**：Webhook 被服务端接收后，用来编排诊断流程的框架。

Webhook 本身不是 LangGraph 搭建的；LangGraph 只负责后半段诊断。

---

## 二、问题 1：主动轮询是怎么实现的？是不是一直有 Python 脚本在后台询问？

### 回答

是的。当前实现就是启动一个常驻 Python 进程，在无限循环中每隔 5 秒执行一次检查。

Windows 启动脚本使用 `start /min` 将它最小化运行：

```bat
start "LHM Alert Agent" /min %PYTHON_CMD% scripts/lhm_alert_agent.py
```

Agent 的主循环如下：

```python
while True:
    proc = find_process(MONITOR_PROCESS)
    snapshot = collect_system_snapshot()
    write_light_snapshot(snapshot)

    if ONCALL_TEMP_ENABLED:
        sensor = fetch_temperature(LHM_SENSOR_NAME_CONTAINS)

    send_heartbeat(
        current_temp_val,
        recent_points,
        snapshot.get("cpu_percent"),
        snapshot.get("memory_percent"),
        snapshot.get("top_processes", []),
    )

    time.sleep(POLL_INTERVAL_SEC)
```

配置默认值：

```python
POLL_INTERVAL_SEC = int(os.getenv("POLL_INTERVAL_SEC", "5"))
```

### 每轮具体做什么

1. 使用 `psutil.process_iter()` 遍历系统进程，寻找 `MATLAB.exe`。
2. 使用 `psutil.cpu_percent()` 和 `psutil.virtual_memory()` 采集系统 CPU、内存。
3. 对进程按 CPU 排序，记录前 10 个高占用进程。
4. 温度开关打开时，通过 LibreHardwareMonitor 的 `/data.json` 接口读取温度。
5. 将最新快照写入 `memory/emergency/last_snapshot.json`。
6. 向 FastAPI 的 `/api/heartbeat` 发送心跳和系统状态。
7. 睡眠 5 秒后进入下一轮。

### “主动”的含义

这里的主动不是 MATLAB 主动通知系统，而是 Agent 主动、周期性地查看 MATLAB 是否还在。它与事件订阅方式不同：

- 轮询：实现简单、兼容性好，但检测延迟最多接近一个轮询周期。
- 操作系统事件订阅：延迟更低，但 Windows/Linux 实现不同，开发和部署更复杂。

本项目使用轮询是一个合理的工程原型选择。

### 面试时可以怎么说

> 我在本机运行了一个轻量 Python Agent，通过 `psutil` 每 5 秒检查 MATLAB 进程并采集系统指标，同时用心跳把最近状态上报给 FastAPI。它不是定时任务每次重新启动，而是一个常驻循环进程。

对应代码：`scripts/lhm_alert_agent.py:463-643`、`start-windows.bat:92`。

---

## 三、问题 2：crash dump、系统快照和 Webhook 分别是什么？何时触发？是否基于 LangGraph？

### 2.1 crash dump 是什么

Crash dump 是程序发生严重异常时保存的故障现场文件或崩溃日志。它可能包含：

- 异常类型，例如访问违规、段错误、栈溢出；
- 出错线程；
- 调用栈；
- 模块和地址；
- 寄存器、内存映射等底层信息。

可以把它理解为“程序崩溃现场的黑匣子”。

### crash dump 一定是崩溃后才产生的吗

通常是。它一般由 MATLAB、操作系统或事先配置的转储工具在异常发生时生成。但是需要注意：

- 正常退出通常不会产生 crash dump；
- 并不是每次崩溃都一定能成功生成 dump；
- 如果 dump 配置未启用、目录配置错误或进程被强制杀死，可能没有 dump；
- 当前项目读取的是 MATLAB crash dump 文本，不是自己在 Python 中生成完整内存转储。

当前代码只是到配置目录中找最新的匹配文件：

```python
def find_latest_crash_dump(crash_dir: str, pattern: str) -> str | None:
    search_pattern = os.path.join(expanded, pattern)
    files = glob_mod.glob(search_pattern)
    if not files:
        return None
    return max(files, key=os.path.getmtime)
```

随后用规则提取异常类型和调用栈：

```python
keywords_map = [
    ("stack overflow", "stack_overflow"),
    ("access violation", "access_violation"),
    ("segmentation violation", "segmentation_violation"),
    ("assertion failed", "assertion_failed"),
    ("unexpected exception", "unexpected_exception"),
    ("out of memory", "out_of_memory"),
]

if "STACK TRACE" in upper_line or "CALL STACK" in upper_line:
    stack_section_started = True
```

### 当前 dump 关联逻辑的局限

代码只判断 dump 的修改时间是否大于 `last_crash_mtime`，没有判断它是否真的在本次进程消失前后生成。程序第一次检测到 MATLAB 退出时，`last_crash_mtime` 初始值为 0，因此目录中的旧 dump 也可能被误当成本次 dump。

更可靠的做法是：

```text
进程消失时间 - 容差 <= dump 修改时间 <= 进程消失时间 + 容差
```

例如只接受进程消失前后 30 秒内生成的 dump，并同时记录 PID、文件大小和退出码。

### 2.2 系统快照是谁截的

是 Agent 自己通过 `psutil` 采集的数据，不是屏幕截图，也不是操作系统完整内存快照。

```python
snapshot["cpu_percent"] = round(psutil.cpu_percent(interval=0.5), 1)
vm = psutil.virtual_memory()
snapshot["memory_percent"] = round(vm.percent, 1)
snapshot["memory_used_gb"] = round(vm.used / (1024 ** 3), 2)
snapshot["memory_total_gb"] = round(vm.total / (1024 ** 3), 2)
snapshot["top_processes"] = top_procs
```

系统快照包含：

- 时间与主机名；
- 系统 CPU 使用率；
- 内存使用率、已使用容量和总容量；
- CPU 占用最高的一组进程。

需要准确描述时间关系：

- `last_snapshot.json` 每 5 秒覆盖一次，比较接近“崩溃前最近状态”；
- Webhook 中的 `system_snapshot` 是发现 MATLAB 已消失后调用 `collect_system_snapshot()` 得到的，严格说是“退出检测时快照”，不一定是崩溃瞬间；
- `last_breath.json` 也是进程消失后写入的，只是文件名叫“last breath”。

因此简历中建议写“附带最近系统快照”，不要说成“精确捕获崩溃瞬间现场”。

### 2.3 Webhook 是什么

Webhook 本质上是“事件发生后，主动向指定 HTTP 地址发送请求”。

本项目发生温度超阈或进程消失事件后，Agent 向 FastAPI 发送 HTTP POST：

```python
url = f"{FASTAPI_BASE_URL}/api/alerts/ingest"
headers = {"Authorization": f"Bearer {ALERT_WEBHOOK_TOKEN}"}
resp = requests.post(url, json=payload, headers=headers, timeout=10)
```

其中 JSON payload 包含告警名称、主机、严重级别、指标、dump 摘要和系统快照。

与轮询的关系是：

```text
Agent 用轮询发现事件
        ↓
Agent 用 Webhook 把事件推给 FastAPI
        ↓
FastAPI 决定发邮件、是否启动诊断
```

### 2.4 Webhook 是基于 LangGraph 搭建的吗

不是。

- Webhook 接口由 FastAPI 实现：`POST /api/alerts/ingest`。
- LangGraph 位于接口后面，用来编排诊断步骤。

```python
record, is_new = await alert_service.ingest(req)

if is_new and req.severity == "critical" and config.oncall_auto_diagnosis:
    await alert_service.trigger_diagnosis(record)
```

LangGraph 的流程是：

```python
workflow.add_edge("planner", "executor")
workflow.add_edge("executor", "microcompact")
workflow.add_edge("microcompact", "replanner")
```

### 2.5 是达到一定条件才触发吗

有两条触发路径。

#### 温度告警

只有温度监控开关打开，并且温度连续超过阈值达到指定时长，才发送 Webhook：

```python
if value >= ONCALL_TEMP_THRESHOLD_C:
    if breach_start is None:
        breach_start = now
    elapsed = now - breach_start
    if elapsed >= ONCALL_TEMP_DURATION_SEC and not in_cooldown:
        send_webhook(sensor, value, recent_points)
```

默认阈值是 85°C、持续 60 秒、告警后冷却 120 秒。不过当前温度监控默认关闭。

#### MATLAB 进程退出

只要发生“上一轮存在、当前轮不存在”的状态变化，就立即发送 critical Webhook，不要求一定存在 dump：

```python
if proc:
    process_was_alive = True
elif process_was_alive:
    snapshot = collect_system_snapshot()
    # 尝试读取 dump；找不到也继续告警
    send_process_exit_alert(MONITOR_PROCESS, crash_info, snapshot)
    process_was_alive = False
```

---

## 四、问题 3：会误发邮件吗？MATLAB 正常关闭会不会触发？

### 结论

**会。当前版本中，MATLAB 正常关闭也会触发告警邮件，而且会被标记为 critical。**

原因是代码只观察到了：

```text
上一次轮询 MATLAB 存在
当前轮询 MATLAB 不存在
```

它没有判断：

- 用户是否点击了正常退出；
- MATLAB 的退出码；
- 是否确实生成了新 crash dump；
- Windows 事件日志里是否记录了 Application Error；
- 是否处于维护窗口。

对应代码甚至已经提示这种情况：

```python
print("未找到崩溃日志，可能进程是被手动结束的")
```

但提示之后仍然执行：

```python
send_process_exit_alert(MONITOR_PROCESS, crash_info, snapshot)
```

### 邮件和自动诊断如何触发

服务端收到一条新告警后，不论 warning 还是 critical，都会后台发送首次告警邮件：

```python
self._alerts[alert_id] = record
asyncio.create_task(self._send_alert_email(record))
```

只有 critical 告警会进一步触发 LangGraph 自动诊断，并可能发送第二封诊断报告邮件：

```python
if is_new and req.severity == "critical" and config.oncall_auto_diagnosis:
    await alert_service.trigger_diagnosis(record)
```

MATLAB 退出告警被固定设置成 critical，所以自然关闭也会触发自动诊断。

### 当前已有的抑制措施

1. 温度告警有持续时间阈值，避免一次瞬时尖峰触发。
2. 温度告警有 120 秒冷却时间。
3. 服务端按 `host + sensor_id/alert_name` 生成稳定告警 ID。
4. 相同 ID 的告警仍为 active 时，重复上报不会再次发邮件。

```python
raw = f"{req.host}|{req.sensor_id or req.alert_name}"
alert_id = hashlib.sha1(raw.encode()).hexdigest()[:12]

if existing and existing.status == "active":
    return existing, False
```

### 这里还有一个反向问题

MATLAB 退出告警不会自动恢复，除非手动调用 resolve 接口。因此第一次退出后，该告警会一直处于 active；后续再次启动并退出，可能被当成同一条重复告警而不再发邮件。

### 更合理的改进方案

建议把事件分成三类：

```text
进程消失 + 新 dump/系统崩溃事件     → critical，发邮件并自动诊断
进程消失 + 无 dump + 非零退出证据   → warning/critical，发邮件
进程消失 + 无异常证据               → info，记录但不发邮件
```

还可以增加：

- 在用户正常关闭前设置维护标记；
- 读取 Windows Event Viewer 的 Application Error 事件；
- 使用进程句柄获取退出码，而不是只按进程名轮询；
- 只接受进程退出前后短时间窗口内的新 dump；
- MATLAB 再次启动时自动 resolve 上一次进程退出告警。

### 面试时应主动承认

> 当前原型使用进程状态边沿触发，因此自然关闭会产生假阳性。我通过持续阈值、冷却和服务端去重控制重复告警，但要区分正常退出和崩溃，还需要结合退出码、新 dump 时间窗口及 Windows 事件日志。

这种回答比声称“不会误报”更可信。

---

## 五、问题 4：H1/H2 是什么？Parent-Child 是怎么切的？

### H1/H2 是 Markdown 标题层级

Markdown 中：

```markdown
# MATLAB 内存故障排查          ← H1

## Out of Memory              ← H2

### 诊断命令                   ← H3
```

项目只把 H1 和 H2 当作 parent 边界，没有按 H3 再切，避免章节过碎：

```python
self.markdown_splitter = MarkdownHeaderTextSplitter(
    headers_to_split_on=[
        ("#", "h1"),
        ("##", "h2"),
    ],
    strip_headers=False,
)
```

### Parent-Child 的两阶段切分

你的理解基本正确：**先按标题分成 parent，再在每个 parent 内切 child。**

```text
一份 Markdown 文档
  ├─ Parent 1：H1/H2 章节 A
  │    ├─ Child A1：≤ 420 tokens
  │    ├─ Child A2：≤ 420 tokens
  │    └─ Child A3：≤ 420 tokens
  └─ Parent 2：H1/H2 章节 B
       ├─ Child B1
       └─ Child B2
```

代码先获得 parent：

```python
parent_docs = self.markdown_splitter.split_text(content)
```

然后为每个 parent 单独创建 child splitter：

```python
for parent_index, parent in enumerate(parent_docs):
    parent_content = parent.page_content.strip()
    child_docs = child_splitter.create_documents(
        [parent_content], [dict(parent.metadata)]
    )
```

Child 不跨 parent，因此不会把“内存溢出”章节末尾与“显卡驱动崩溃”章节开头拼在一起。

### 标题上下文如何保留

每个 child 前面都会补上文档标题和章节路径：

```python
context_prefix = "\n".join(
    part for part in (
        f"文档：{document_title}",
        f"章节：{header_path}",
    ) if part
)

doc.page_content = f"{context_prefix}\n\n{child_content}"
```

同时在 metadata 中保存 parent 和 child：

```python
{
    "_parent_id": f"{Path(file_path).name}:{parent_index}",
    "_parent_content": parent_content,
    "_child_content": child_content,
    "_child_index": child_index,
}
```

### 为什么这样做

- 检索时用短 child：主题更集中，匹配更准。
- 生成答案时恢复完整 parent：上下文更完整，避免只拿到一句话。

当前代码在返回检索结果时会把匹配 child 替换成 parent 内容：

```python
parent_content = str(metadata.get("_parent_content") or doc.page_content)
metadata["_matched_child"] = str(
    metadata.get("_child_content") or doc.page_content
)
return Document(page_content=parent_content, metadata=metadata)
```

因此准确说法是：**child 用于召回，parent 用于生成上下文。**

---

## 六、问题 5：为什么按 token 切分，不按字符？420 是怎么得到的？

### 为什么按 token

Embedding 模型和 LLM 实际处理的是 token，不是 Python 字符数。

同样的字符数量不一定对应同样的 token 数：

- 英文单词可能由一个或多个 token 组成；
- 中文字符与 token 也不是固定的一比一关系；
- 错误码、路径、十六进制地址可能被拆成多个 token；
- 不同 tokenizer 的切分结果不同。

如果按字符切，可能出现两个问题：

1. 看起来字符不多，实际 token 已超过 embedding 模型限制，被截断。
2. 不同语言的 chunk 信息量差异很大。

项目直接使用 BGE 模型自己的 tokenizer：

```python
self.tokenizer = AutoTokenizer.from_pretrained(
    config.dashscope_embedding_model,
    cache_dir=".hf-cache",
)

self.text_splitter = RecursiveCharacterTextSplitter.from_huggingface_tokenizer(
    self.tokenizer,
    chunk_size=self.chunk_size,
    chunk_overlap=self.chunk_overlap,
)
```

这保证“420 tokens”与实际 embedding 模型的输入口径一致。

### 420 为什么不是 512

`BAAI/bge-small-zh-v1.5` 常用最大序列长度是 512 tokens。项目没有把 child 直接设置为 512，而是留出余量给：

- 文档标题；
- H1/H2 章节路径；
- tokenizer 的特殊 token；
- 边界和格式文本。

代码还会根据标题前缀实际占用的 token 数进一步缩小正文 child：

```python
prefix_tokens = len(
    self.tokenizer.encode(context_prefix, add_special_tokens=False)
)

chunk_size=max(64, self.chunk_size - prefix_tokens - 2)
```

因此最终“标题前缀 + child”控制在约 420 tokens 内，而不是正文 420 后再追加标题。

### 420 是严格实验得出的吗

从当前仓库看，**没有 256/320/420/480 等 chunk size 的系统消融实验记录**。420 更像是一个工程超参数：在 512 上限下保留约 18% 余量，同时保持足够完整的故障步骤。

所以面试时不要说“实验验证 420 是最优值”，更准确的说法是：

> 420 是考虑 BGE 512-token 输入上限、标题前缀和内容完整性后选择的初始值，当前检索评测是在该配置下完成的；后续可通过 chunk-size 消融实验继续调优。

### 64-token overlap 的作用

相邻 child 保留 64-token 重叠，用来降低关键信息刚好被切在边界两侧的概率。重叠只发生在同一个 parent 内，不跨 H1/H2 章节。

重叠过大会增加：

- 向量数量；
- 索引空间；
- 相似重复结果；
- 检索去重压力。

因此 overlap 也是召回完整性与重复度之间的折中。

---

## 七、问题 6：BGE、Dense、BM25、稀疏向量和稠密向量是什么关系？

### 7.1 BGE 是什么

BGE 是 BAAI General Embedding 系列模型。当前项目使用：

```text
BAAI/bge-small-zh-v1.5
```

它把查询或文档编码成 512 维稠密向量。当前项目在 CPU 本地运行，并对向量归一化：

```python
vector_embedding_service = HuggingFaceEmbeddings(
    model_name=config.dashscope_embedding_model,
    model_kwargs={"device": "cpu"},
    encode_kwargs={"normalize_embeddings": True},
)
```

### 7.2 Dense 和 BM25 是两条独立召回路径吗

是的。

```text
查询
  ├─ BGE 编码 → Dense 向量检索
  └─ 中文分词/词频统计 → BM25 稀疏检索
                       ↓
                    RRF 融合
```

项目在同一个 Milvus collection 中保存两类字段：

```python
vector_field=["dense", "sparse"]

builtin_function=BM25BuiltInFunction(
    input_field_names="content",
    output_field_names="sparse",
    analyzer_params={"type": "chinese"},
)
```

### 7.3 稠密向量是什么

稠密向量一般是类似下面的固定维度浮点数组：

```text
[0.013, -0.082, 0.117, ..., 0.026]  # 512 维，大部分位置非零
```

它由神经网络 embedding 模型生成，主要表达语义。即使查询和文档没有使用完全相同的词，只要含义接近，也可能得到较高相似度。

例如：

```text
查询：MATLAB 内存不够怎么办
文档：大规模矩阵导致 out of memory
```

Dense 召回可能理解“内存不够”和“out of memory”语义接近。

### 7.4 BM25 和稀疏向量是什么

BM25 是经典词法检索算法。它根据：

- 查询词是否出现；
- 词在当前文档中的频率；
- 词在整个语料中是否稀有；
- 文档长度；

计算查询与文档的匹配分数。

稀疏表示的维度可以理解成整个词表，绝大多数词在某篇文档中都没有出现，所以绝大多数位置是 0。

### BM25 需要 embedding 吗

**不需要 BGE 这种神经网络 embedding。**

它需要的是：

1. analyzer/tokenizer 把文本切成词；
2. 建立词频和文档频率统计；
3. 用 BM25 公式计算词法相关性。

Milvus 内置 BM25 会把词法统计组织成 sparse vector/index，但这不是调用 BGE 得到的 embedding。可以说它生成“稀疏检索表示”，不要说它执行“神经网络 embedding”。

### 7.5 哪种场景更适合 Dense/BM25

这与“数据本身稠密还是稀疏”没有关系，而与匹配目标有关：

- Dense 更适合语义、同义表达、自然语言描述。
- BM25 更适合错误码、函数名、模块名、日志原词精确命中。

例如 `EXCEPTION_ACCESS_VIOLATION`、`mexw64`、`mxGetPr` 这类标识符，BM25 往往很有价值；“MATLAB 突然闪退”与“进程异常终止”这种同义表达，Dense 往往更强。

双路召回的目的就是互补。

---

## 八、问题 7：RRE/RRF 是什么？Cross-Encoder 是什么？

### 先纠正术语

这里是 **RRF**，不是 RRE。全称是 Reciprocal Rank Fusion，中文通常叫“倒数排名融合”。

### 8.1 RRF 如何融合两路结果

假设 Dense 和 BM25 分别返回一份排名：

```text
Dense：A, B, C, D
BM25： C, A, E, B
```

RRF 不要求两路原始分数处于同一个尺度，只使用名次。常见形式为：

```text
RRF_score(d) = Σ 1 / (k + rank_i(d))
```

某篇文档在多个召回列表中排名越靠前，融合分数越高。

项目配置：

```python
rag_rrf_k: int = 60
```

调用 Milvus 时：

```python
self.vector_store.similarity_search_with_score(
    expand_query(query),
    k=candidate_count,
    fetch_k=max(config.rag_dense_candidates, config.rag_sparse_candidates),
    ranker_type="rrf",
    ranker_params={"k": config.rag_rrf_k},
)
```

这里的 `k=60` 是 RRF 平滑常数，不是最终返回 Top 60。最终 `rag_top_k` 是 5。

### 为什么不直接把 Dense 分数和 BM25 分数相加

因为二者分数分布和量纲不同，直接相加容易让某一路占主导。RRF 只看排名，简单、稳定，也省去了复杂的分数校准。

### 8.2 Cross-Encoder 是什么

你的理解正确。

Bi-Encoder/Dense 召回通常分别编码 query 和 document：

```text
query → query vector
document → document vector
二者计算相似度
```

Cross-Encoder 则把二者组成一对后一起输入模型：

```text
[query, candidate document] → Cross-Encoder → relevance score
```

项目代码：

```python
pairs = [
    (query, str(doc.metadata.get("_child_content") or doc.page_content))
    for doc in documents
]
scores = model.predict(pairs, show_progress_bar=False)
```

当前模型是：

```text
BAAI/bge-reranker-base
```

### 为什么 Cross-Encoder 更准但更慢

Dense 检索可以提前把所有文档向量算好，查询时只需要算一次 query vector 并做向量搜索。

Cross-Encoder 必须针对每一个候选重新执行模型推理。如果有 20 个候选，就要评分 20 个 query-document pair，因此只适合召回后的少量候选重排，不能直接遍历整个知识库。

### 当前项目不是完全用 Cross-Encoder 分数覆盖 RRF

它把归一化后的 Cross-Encoder 分数与初始 RRF 分数融合：

```python
score = weight * cross_score + (1 - weight) * initial_score
```

当前 `weight=0.20`，即 Cross-Encoder 占 20%，初始排名占 80%。然后按文档来源去重并取 Top K。

### 简历指标口径要准确

README 中有两组实验：

| 实验 | Recall@5 | MRR |
|---|---:|---:|
| Dense 原查询 | 68.33% | 0.8121 |
| Dense + BM25 + RRF | 82.92% | 0.8875 |
| 固定 Hybrid + Cross-Encoder | 83.75% | 0.8833 |
| 完整自适应检索 | 85.00% | 0.8979 |

因此 68.33% → 85.00% 是“完整自适应检索链”带来的结果，不应归因于 Cross-Encoder 单个组件。固定 Cross-Encoder 对照的 Recall@5 是 83.75%，MRR 还比纯 Hybrid 略低。

更准确的简历写法可以是：

> 基于 BGE + Milvus 实现 Dense/BM25 双路召回、RRF 融合，并结合置信度驱动的 Query Rewrite 与 Cross-Encoder 重排，使 40 条可回答标注查询的 Recall@5 从 68.33% 提升至 85.00%，MRR 从 0.8121 提升至 0.8979。

---

## 九、问题 8：Query Rewrite 用的是 LLM 还是规则？如何权衡速度？

### 结论

当前项目没有使用 LLM Rewrite，而是两层规则处理。

### 第一层：每次检索都会执行的关键词扩展

项目维护一个故障同义词字典：

```python
_QUERY_EXPANSIONS = {
    "栈溢出": "stack_overflow 无限递归 递归深度 dbstack 迭代替代",
    "访问违规": "access_violation MEX 指针 NULL 野指针 mxGetPr",
    "oom": "内存不足 out_of_memory memory whos 预分配 分块 datastore tall array",
    "cpu": "高负载 profile profiler timeit parfor 计算密集型 性能分析",
}
```

命中关键词时，将相关中英文术语追加到原查询：

```python
def expand_query(query: str) -> str:
    additions = [
        terms
        for key, terms in _QUERY_EXPANSIONS.items()
        if key in query.lower()
    ]
    return " ".join([query, *additions]) if additions else query
```

这一层非常快，只是字典匹配和字符串拼接。

### 第二层：仅在低置信度时执行的一次性 Rewrite

如果首次召回置信度低，系统从长日志中提取：

- 错误码；
- DLL、MEX、SO 文件名；
- MATLAB 函数名；
- 已知故障短语；
- 查询前 3 个非空行，最多 240 字符。

```python
def rewrite_query_for_retry(query: str) -> str:
    markers = sorted(_exact_markers(query))
    phrases = [p for p in _FAULT_PHRASES if p.lower() in query.lower()]
    lines = [line.strip() for line in query.splitlines() if line.strip()]
    compact_line = " ".join(lines[:3])[:240]
    parts = ["MATLAB 运维故障诊断", *markers, *phrases, compact_line]
    return deduplicate(parts)
```

调用方把尝试次数硬限制为最多 2 次：

```python
candidates = self._hybrid_candidates(query, candidate_count, expr)
attempts = 1

if confidence.level == "low" and config.rag_retry_on_low_confidence:
    retry_query = rewrite_query_for_retry(query)
    retry_candidates = self._hybrid_candidates(retry_query, candidate_count, expr)
    attempts = 2
```

如果新查询的置信度没有提高，仍保留原查询结果。

### 速度与效果如何权衡

当前设计的权衡是：

1. 规则扩展几乎没有模型推理开销。
2. 只有低置信度才增加第二次 Milvus 检索。
3. 最多重试一次，避免 Agent 陷入“改写—检索—再改写”的循环。
4. 高置信度直接返回，不运行 Cross-Encoder。
5. 中等置信度才运行较慢的 Cross-Encoder。

与 LLM Rewrite 相比：

| 方案 | 优点 | 缺点 |
|---|---|---|
| 规则 Rewrite | 快、稳定、可解释、无需额外 API | 覆盖范围依赖人工词典 |
| LLM Rewrite | 能理解复杂上下文和隐含语义 | 增加延迟、成本和不确定性，可能改丢错误码 |

对错误码和日志检索而言，规则式 Rewrite 是合理选择，因为最重要的是保留精确标识符，而不是生成更流畅的自然语言。

---

## 十、问题 9：检索置信度怎么计算？低/中/高阈值怎么划分？

### 10.1 置信度组成

当前项目没有直接把 Milvus 的 RRF 分数当作置信度，而是计算四个可解释特征。

#### 1. 精确标识符覆盖率 exact

提取查询中的错误码、全大写标识、DLL/MEX/SO 文件名和 MATLAB 诊断函数，例如：

```text
0xC0000005
EXCEPTION_ACCESS_VIOLATION
module.mexw64
mxGetPr
gpuArray
```

检查这些标识符在召回证据中出现了多少。

#### 2. 词法覆盖率 lexical

对英文、数字以及中文二元词进行简单分词，计算：

```text
查询有效 token 与证据 token 的交集数 / 查询有效 token 数
```

#### 3. 来源集中度 dominance

查看前 5 个候选中，有多少来自排名第一文档的同一来源。它试图表达证据是否集中支持某个来源。

#### 4. 来源可信度 trust

当前映射为：

```python
{"high": 1.0, "internal": 0.9, "medium": 0.75}
```

官方文档标为 high，内部 incident 标为 internal，普通知识或案例通常是 medium。

### 10.2 计算公式

```python
score = (
    0.45 * exact
    + 0.30 * lexical
    + 0.15 * dominance
    + 0.10 * trust_score
)
```

之后将结果裁剪到 `[0, 1]`。

权重表达了当前设计偏好：错误码、模块名等精确标识最重要，其次是普通词法覆盖，然后才是来源集中度和来源可信度。

### 10.3 低、中、高阈值

当前配置：

```python
rag_confidence_low = 0.35
rag_confidence_high = 0.55
rag_semantic_evidence_threshold = 0.55
```

分类逻辑：

```python
level = (
    "high" if score >= 0.55
    else "low" if score < 0.35
    else "medium"
)
```

即：

- `[0, 0.35)`：低置信度；
- `[0.35, 0.55)`：中等置信度；
- `[0.55, 1]`：高置信度。

### 10.4 三档分别做什么

```text
高置信度
  └─ 直接使用 RRF 结果，不运行 Cross-Encoder

中等置信度
  └─ 使用 Cross-Encoder 重排

低置信度
  ├─ 规则 Rewrite，最多再检索一次
  ├─ 用 Cross-Encoder 检查是否存在足够强的语义证据
  └─ 仍无证据时标记 evidence_sufficient=False
```

低置信度查询有一个语义恢复步骤：如果 Cross-Encoder 的最高语义分数达到 0.55，就把它提升为 medium，再进行重排。

### 10.5 “原因未确定”怎么落实

检索 metadata 中会写：

```python
metadata["_evidence_sufficient"] = confidence.level != "low"
```

生成给 LLM 的上下文则明确要求：

```text
证据不足时必须写“原因未确定”，并列出待补充证据。
```

需要注意：这是提示词和元数据约束，不是形式化证明。LLM 仍可能违反要求，生产环境还应增加输出规则校验。

### 10.6 阈值是怎么得出来的

当前 README 明确把它描述为“小型验证集上的起始阈值”。仓库中没有看到完整的阈值网格搜索或校准曲线，因此不应宣称 0.35/0.55 是统计最优值。

更规范的阈值选择方法是：

1. 将数据分为开发集和独立测试集。
2. 在开发集上遍历 low/high 阈值组合。
3. 同时观察可回答问题误拒率、不可回答问题正确拒答率、Recall、重排率和 P95 延迟。
4. 根据业务成本选择 operating point。
5. 最后只在独立测试集上报告一次结果。

当前 10 条不可回答查询的 100% 拒答率只能说明这 10 条样本被正确拒绝，不能泛化成生产环境 100%。

---

## 十一、问题 10：分层记忆是什么？如何实现？有什么好处？

### 11.1 当前项目的 Hot/Cold 分层

诊断完成后，项目将信息保存到不同层：

```text
memory/
  ├─ MEMORY.md              Hot：最近 20 条诊断摘要和链接
  ├─ incidents/             Cold：完整诊断报告
  ├─ artifacts/             大型工具原始输出
  └─ emergency/             监控 Agent 的最近系统快照
```

其中真正的长期诊断记忆主要是 Hot + Cold：

- Hot：`MEMORY.md`，短、经常加载。
- Cold：`incidents/*.md`，完整、默认不直接塞进 Prompt。

### 11.2 诊断完成后如何写入

LangGraph 完成最终报告后调用：

```python
await memory_writer.save_incident(
    session_id=session_id,
    task_description=user_input[:120],
    report=final_response,
)
```

`save_incident()` 做三件事：

#### 1. 完整报告写入 Cold

```python
incident_path = os.path.join(
    "memory/incidents",
    f"{timestamp}_{session_id}.md",
)
await f.write(incident_content)
```

#### 2. 启发式摘要写入 Hot

摘要不调用 LLM，而是从任务描述和报告前部提取最多约 220 字符，写入 `MEMORY.md` 表格，并只保留最近 20 条。

```python
_MAX_RECENT_INCIDENTS = 20
_MAX_SUMMARY_CELL_CHARS = 220
```

#### 3. 完整 incident 再索引进 Milvus

```python
await asyncio.to_thread(
    vector_index_service.index_single_file,
    incident_path,
)
```

这样历史事故既能通过 Hot 摘要被直接看到，也能通过 RAG 按语义和关键词召回。

### 11.3 如何读取 Hot 记忆

Planner 和普通 RAG Agent 每轮读取 `MEMORY.md`，最多注入 4000 字符：

```python
def load_memory_context(max_chars: int = 4000) -> str:
    content = f.read().strip()
    combined = f"### 历史诊断索引 (MEMORY.md)\n\n{content}"
    return combined[:max_chars]
```

Cold 完整报告和 artifacts 默认不整批注入 Prompt，避免撑爆上下文。

### 11.4 分层记忆的好处

1. **跨会话保留经验**：服务重启或新会话仍能使用历史事故。
2. **控制上下文大小**：只常驻短摘要，不把所有完整报告塞给 LLM。
3. **保留可追溯性**：摘要中包含完整报告链接，需要时可回看原文。
4. **支持语义召回**：Cold incident 进入 Milvus 后，可以按当前故障召回相似历史事故。
5. **成本可控**：摘要为规则截取，不需要额外调用 LLM。

### 当前实现的边界

- `MEMORY.md` 是最近 20 条的时间索引，不是按当前问题动态挑选的 relevant memory。
- 每轮直接加载 Hot 文件，条目多时只按字符截断，可能截到表格中间。
- 历史报告重新入库后，需要处理重复索引、过期经验和错误诊断污染。
- 当前没有自动判断某条记忆是否已经失效。

因此可以称为“Hot/Cold 分层记忆原型”，但不要描述成完整的企业级记忆治理系统。

---

## 十二、问题 11：Microcompact 是什么？与分层记忆有什么区别？能否结合？与 Claude Code 的记忆系统相似吗？

### 12.1 Microcompact 解决什么问题

Microcompact 解决的是**当前一次 Agent 执行中，某个工具输出太长**的问题。

例如日志工具一次返回几万字符，如果把完整结果一直保留在 `past_steps` 中，Replanner 每一轮都要重复携带这些 token，会增加延迟和成本，甚至撑满上下文窗口。

它位于 Executor 与 Replanner 之间：

```text
Executor 产生工具结果
        ↓
Microcompact 检查是否过长
        ↓
Replanner 只看到摘要和原文路径
```

LangGraph 连线：

```python
workflow.add_edge(NODE_EXECUTOR, NODE_MICROCOMPACT)
workflow.add_edge(NODE_MICROCOMPACT, NODE_REPLANNER)
```

### 12.2 当前代码如何压缩

当前演示阈值是估算 100 tokens。超过阈值后：

1. 将原始工具结果写入 `memory/artifacts/{session_id}/`；
2. 保留开头 200 字符；
3. 抽取最多 5 条包含 ERROR、EXCEPTION、TRACEBACK、FATAL 等关键词的行；
4. 保留结尾 200 字符；
5. 用摘要和 artifact 路径替换 `past_steps` 中的原始结果。

```python
if before_tokens > _MICROCOMPACT_TOKEN_THRESHOLD:
    artifact_path = await _save_artifact(
        session_id, last_step, str(last_result)
    )
    preview = _smart_preview(raw_text)
    compact_result = (
        f"[Microcompact] 原始结果已压缩落盘 → {artifact_path}\n"
        f"{preview}"
    )
```

这里的 token 数不是 BGE tokenizer 精确计数，而是 `字符数 // 4` 的近似值：

```python
_CHARS_PER_TOKEN = 4
return max(1, len(text) // _CHARS_PER_TOKEN)
```

所以 100-token 阈值只是演示参数，中文内容下误差可能较大。

### 12.3 Microcompact 与长期记忆的区别

| 维度 | Microcompact | 分层长期记忆 |
|---|---|---|
| 主要目标 | 缩小当前会话工具上下文 | 跨会话保存诊断经验 |
| 触发时间 | 每个 Executor 步骤之后 | 一次诊断完成之后 |
| 原始数据 | 日志、工具返回等中间结果 | 最终诊断报告 |
| 热数据 | 头部 + 错误行 + 尾部摘要 | `MEMORY.md` 事故摘要 |
| 冷数据 | `artifacts/` 原始工具输出 | `incidents/` 完整报告 |
| 是否进入 Milvus | 当前没有 | incident 会进入 Milvus |
| 生命周期 | 主要服务于当前执行 | 服务于后续会话和相似事故 |

Microcompact 更接近“上下文虚拟内存/工具结果卸载”，分层记忆更接近“长期经验库”。

### 12.4 两者可以结合吗

可以，而且当前项目已经做了基础结合：

```text
当前会话
  工具原始输出 → artifacts（冷）
  工具摘要     → past_steps（热）
                      ↓
               生成最终诊断报告
                      ↓
跨会话
  完整报告     → incidents（冷）+ Milvus
  报告摘要     → MEMORY.md（热）
```

更完整的结合方式还可以包括：

- Replanner 发现摘要不够时，按 artifact 路径局部读取原文；
- 只把最终确认有价值的 artifact 关联到 incident；
- 为 artifact 建立字段索引，而不是把所有原始日志都向量化；
- 对长期记忆设置来源、时间、置信度和失效规则。

### 12.5 与 Claude Code 的记忆/压缩机制有什么相似之处

这里没有阅读整份泄露源码，只参考了 Anthropic 官方文档和一篇聚焦记忆系统的公开解析。

Anthropic 官方文档公开说明：

- Claude Code 使用 `CLAUDE.md` 和 auto memory 跨会话保存信息；
- 上下文接近上限时，会先清理旧工具输出，再对会话进行摘要压缩；
- project-root `CLAUDE.md` 会在 compact 后重新注入。

参考：

- [Claude Code 官方：How Claude remembers your project](https://code.claude.com/docs/en/memory)
- [Claude Code 官方：How Claude Code works](https://code.claude.com/docs/en/how-claude-code-works)

公开解析文章将 Claude Code 的相关机制概括为长期记忆、相关记忆、会话摘要和子代理记忆等不同层，强调文件系统优先、不同记忆职责分开：

- [潘锦：深入 Claude Code 源码了解其记忆系统](https://www.phppan.com/2026/04/claude-code-source-memory/)

### 相似点

1. **文件系统作为外部存储**：完整信息落盘，Prompt 中只保留摘要或索引。
2. **区分长期记忆与会话压缩**：跨会话经验和当前上下文不是同一件事。
3. **热/冷分层**：经常需要的信息保持短小，完整原文按需读取。
4. **工具结果优先压缩**：大型工具输出比用户目标更适合先卸载。
5. **摘要中保留指针**：需要时可以根据路径回看原文。

### 不同点

1. 本项目的 Microcompact 是固定规则“头 + 错误行 + 尾”，不是基于模型理解生成的会话摘要。
2. 本项目的 Hot memory 是最近 20 条固定索引，不会根据当前问题动态挑选最相关的几条记忆。
3. 本项目没有独立的子代理记忆作用域。
4. 本项目没有完整的记忆提取、去重、更新、冲突解决和失效机制。
5. 本项目的所谓 Autocompact 主要打印统计；真正改变 `past_steps` 的是 Microcompact 和 Replanner 的 `_collapse_past_steps()`。

因此，最稳妥的说法是：

> 两者在“将大对象落盘、热上下文保留摘要和指针、区分会话压缩与长期记忆”这一设计思想上相似，但我的项目是面向 AIOps 场景的简化实现，并不是对 Claude Code 完整记忆系统的复刻。

### 12.6 `_collapse_past_steps()` 与 Microcompact 的关系

项目实际上还有第二种当前会话压缩：

- Microcompact：压缩单个超长工具结果。
- Collapse：当步骤变多时，压缩较旧步骤，最近两步保留更多细节。

```python
older = past_steps[:-2]
recent = past_steps[-2:]

older_lines = [
    f"[折叠] 步骤{i + 1}: {step} → {str(result)[:120]}..."
    for i, (step, result) in enumerate(older)
]
```

可以把它们理解为：

```text
Microcompact：纵向压缩一个很大的步骤结果
Collapse：横向压缩很多历史步骤
长期记忆：把本次任务的最终经验保存给未来会话
```

---

## 十三、LangGraph Plan-Execute-Replanner 到底做了什么

虽然这不是单独编号的问题，但它贯穿简历描述，需要能够讲清楚。

### Planner

Planner 读取：

- 原始告警任务；
- Hot 长期记忆；
- 初步知识库召回；
- 本地工具和 MCP 工具描述；

然后让 LLM 输出结构化步骤列表。

### Executor

Executor 每次取计划中的第一步，允许 LLM 调用：

- `retrieve_knowledge`：知识库召回；
- `search_log`：本地日志检索；
- `get_current_time`；
- MCP 暴露的实时 CPU、内存、温度等工具。

执行结果加入 `past_steps`。

### Microcompact

如果刚产生的工具结果太长，原文落盘，`past_steps` 中换成结构化预览。

### Replanner

Replanner 根据已完成步骤和剩余计划选择：

```text
continue：继续当前计划
replan：调整剩余计划
respond：证据已经足够，生成最终响应
```

同时设有边界：

- 最多执行 8 步；
- 已执行 5 步后禁止再次 replan；
- 新计划不能比当前剩余计划更长。

这就是“有界 Agent”：允许动态调整，但不给它无限规划和无限工具调用的空间。

---

## 十四、建议采用的最终简历表述

原表述总体方向正确，但建议改为下面的版本，使指标归因和实现边界更准确：

> 开发常驻主动轮询 Agent，每 5 秒通过 psutil 与 LibreHardwareMonitor 采集 CPU、内存、温度和进程状态；检测 MATLAB 从存活变为消失后，关联最近 crash dump、解析调用栈并附带最近系统快照，通过 Webhook 触发告警去重、邮件通知和自动诊断。
>
> 采用 Markdown H1/H2 Parent-Child 与 BGE tokenizer 420-token 切分构建知识库，基于 BGE + Milvus 实现 Dense/BM25 双路召回和 RRF 融合，并结合置信度驱动的一次性 Query Rewrite 与 Cross-Encoder 重排，使 40 条可回答标注查询的 Recall@5 从 68.33% 提升至 85.00%，MRR 从 0.8121 提升至 0.8979。
>
> 设计可解释检索置信度，综合精确错误标识覆盖、词法覆盖、来源集中度和来源可信度划分低/中/高置信度；低置信度最多重试一次，中等置信度触发重排，证据不足时要求输出“原因未确定”并列出待补充证据。
>
> 基于 LangGraph 构建有界 Plan-Execute-Microcompact-Replanner 工作流，编排日志检索、知识召回与 MCP 实时监控工具；将长工具输出落盘并保留结构化摘要，同时以 Hot 摘要索引、Cold 完整报告和 Milvus 历史事故召回实现分层记忆。

### 面试中必须主动说明的限制

1. 进程消失不等于崩溃，MATLAB 正常关闭目前也会告警。
2. Webhook 的系统快照是退出检测时采集，`last_snapshot.json` 才更接近退出前状态。
3. 420 tokens 和置信度阈值是当前验证集上的工程起点，不是经过大规模统计证明的全局最优参数。
4. Query Rewrite 是规则式，不是 LLM Rewrite。
5. 85% 来自完整自适应检索链，不是 Cross-Encoder 单独带来的提升。
6. Microcompact 是会话内上下文压缩，分层记忆是跨会话经验保存，两者职责不同但可以组合。

---

## 十五、快速问答版

### Q：轮询会不会很耗 CPU？

5 秒一次通常开销很低，但当前 `collect_system_snapshot()` 内部调用 `psutil.cpu_percent(interval=0.5)`，每轮会阻塞约 0.5 秒。语料和监控规模扩大后应测量 Agent 自身资源占用。

### Q：为什么不用 MATLAB 自己上报？

如果 MATLAB 直接崩溃，它未必有机会执行上报逻辑。外部 Agent 与被监控进程解耦，MATLAB 消失后 Agent 仍然存活，可以继续收集证据和上报。

### Q：没有 dump 还能诊断吗？

可以，但只能基于进程消失、最近 CPU/内存、系统日志和知识库进行有限推断，结论应写“原因未确定”，并要求补充 dump、退出码或事件日志。

### Q：BM25 也是向量数据库检索吗？

在当前工程中由 Milvus 执行，并以 sparse vector/index 形式存储和搜索；但它不是 BGE 神经网络 embedding，而是词法统计检索。

### Q：为什么先召回再重排？

召回负责快速把全库缩小到约 20 个候选，Cross-Encoder 再对这些候选精细评分，从而平衡速度和精度。

### Q：为什么高置信度反而不重排？

高置信度意味着错误码、关键词和可信来源已经形成较强证据，继续调用 CPU Cross-Encoder 的边际收益可能不值得数秒延迟。

### Q：分层记忆是不是数据库缓存？

思想相似，但这里主要是上下文管理：Hot 层用于频繁注入 Prompt，Cold 层保存完整原文，Milvus负责按当前故障召回历史 incident。

### Q：Microcompact 会不会丢信息？

Prompt 中确实丢掉了中间大部分原文，但原始工具结果仍保存在 artifact 文件中。当前不足是 Agent 没有完整实现“发现摘要不足后自动局部回读 artifact”的闭环。
